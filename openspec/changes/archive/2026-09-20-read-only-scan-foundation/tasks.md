# Implementation Tasks: Read-Only Scan Foundation

Implement the approved read-only slice as sequential, single-writer work units. Historical attempted rows remain implementation evidence, not acceptance. Independent verification rejected checked WU5C1B2, WU5C1B2B, and WU5C1B2B1; they satisfy no dependency. WU5C1B2B1A remains the accepted verification prerequisite. Verifier `subtask_gentle-ai-verify_1788116436728_35d0b50f` rejected the restored WU5C1C candidate because its fake ELOOP did not synchronize a real ancestor/final replacement, its mount poison script stopped at device 9 without observing 7→9→7 or the poison child, cancellation masked descriptor-limit outcomes, and its close matrix omitted required primary/secondary dimensions. Under explicit user reset authorization, the four unchecked WU5C1C rows were superseded by verification-first WU5C1C1 and WU5C1C2; the failed +123-line candidate was restored with no acceptance. Verifier `subtask_gentle-ai-verify_1788117808925_f5d42ba5` subsequently rejected checked WU5C1C1: exact accounting is +233/-0 rather than +226/-0, the real replacement tests do not explicitly stat/fstat and compare renamed-original versus replacement identities after rename, and the mount test does not assert consumed trace exactly `[7,9]` with the final scripted `7` slot explicitly unconsumed. WU5C1C1 remains checked historical verification evidence, independently failed/not accepted, and satisfies no dependency; WU5C1C1A is its accepted minimal test-only correction. Verifier `subtask_gentle-ai-verify_1788119235934_81a16f3d` then rejected checked WU5C1C2: exact C2 accounting is +116/-0 rather than +112/-0, reached-cancellation rows lack explicit exact trace/open-count and immediate no-deeper operation/open assertions, and the lifecycle inventory does not explicitly map every primary-secondary dimension to accepted B1A/B2A/B2B1A/C1A evidence or one of C2's two new rows. C2 remains checked historical verification evidence, independently failed/not accepted, and satisfies no dependency; WU5C1C2A is the minimal test-only verification correction before enumeration. Every accepted work unit remains capped at 400 authored lines, leaves the repository compiling, and assumes no commit or PR.

Verifier `subtask_gentle-ai-verify_1788121542056_f459f57c` rejected checked WU5C2A because the Darwin directory-record parser and valid-record helper accepted a record with no terminating NUL when `reclen == nameOffset+namlen`. WU5C2A remains checked historical implementation evidence only, is not accepted, and satisfies no dependency. WU5C2A1 corrected that defect at exact accounting **+70/-3=73**, with final hashes `walker_darwin.go` `412a412f0e2554d05f9198bdf6381a4fd75a08f7c01eb21f161ceefbc99de020` and `walker_darwin_test.go` `7809445656752727c40f8d4be6bbbdd11c8f78d3a91437d88930a7bcf9cf7d00`; semantic verifier `subtask_gentle-ai-verify_1788122606468_ea94f7a9` passed and the user-authorized reset/adjudication accepts A1 as the split prerequisite. The untouched combined WU5C2B was blocked before any Go/test edit after an honest **550–810 authored-line** forecast, and is superseded by WU5C2B1 then WU5C2B2.

Verifier `subtask_gentle-ai-verify_1788140109519_c2f80e1f` independently rejected checked WU5D2 because the scanner consumes directory facts only through a private fake-only type assertion that the production Darwin `DescriptorWalker` does not implement, and the fact model does not preserve enumerated kind separately from opened final kind. WU5D2 remains checked historical independently failed/non-accepting evidence at exact accounting **+257/-1=258** with final hashes `scanner.go` `bea7c0fa480b1eaa81cbe2c2faf597689ff278e0d2720ebb5a9b3d38fc274126`, `scanner_test.go` `7d4745598308b2e31b042ada221d45e1b26776079e7cd56f3d549ed1afc1289a`, `finalize.go` `2ffd49fcdd5011fe5012bdee31332468eeb2f0afc42a136c39ad8dfbc36c0479`, and `finalize_test.go` `2ec604ae858ecad200e58a6ffc083898a35a0050c070fcc66fa500e614db3719`; it satisfies no dependency. Verifier `subtask_gentle-ai-verify_1788141318866_d33bc6e5` independently rejected checked WU5D2A because Darwin added a separate single-level `WalkFacts` enumeration beside the existing recursive `walkDirectory`, so nested facts were not emitted and one scan could duplicate traversal. WU5D2A remains checked historical independently failed/non-accepting evidence at exact accounting **+196/-43=239** and satisfies no dependency. WU5D2A1 is now accepted from canonical baseline `/tmp/osdy-wu5d2a1-baseline-zGhBuB` at independently reproduced **+270/-97=367**, within the 400-line cap. Functional/source/accounting verifier `subtask_gentle-ai-verify_1788143365237_f68de7e6` passed except for initially missing lens/LSP evidence; parent-native `lens_diagnostics mode=all` then reported zero blockers with one unchanged dispositioned false-positive warning, LSP reported zero errors, and strict adjudicator `subtask_gentle-ai-verify_1788143523521_bbd9b818` passed. D2 and D2A remain failed history. The untouched WU5E1 was blocked before any product/test edit at a detailed **600–925 authored-line** forecast; its four unchecked rows remain historical nonexecuting context. WU5E1A is accepted at the exact **+400/-0=400** cap after independent verifier `subtask_gentle-ai-verify_1788146646060_00f9245a` PASS, parent LSP **0 errors**, clean parent lens diagnostics, and settlement state `complete`. WU5E1B1 is checked historical independently failed evidence; WU5E1B2 is its strict-TDD successor, and WU5E2 remains blocked on independently accepted WU5E1B2.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | 7,428–10,803 authored lines, excluding generated `go.sum` |
| Estimated authored additions | 7,205–10,045 |
| Estimated authored deletions | 223–758 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | WU1 → WU2A → WU2B → WU3 → WU4A → WU4B (historical) → WU4C (historical failed) → WU4D1 → WU4D2 → WU4E → WU5C1A → WU5C1A2 → WU5C1B1 → WU5C1B1A → WU5C1B2 (historical failed) → WU5C1B2A → independent verification → WU5C1B2B (historical verification-only; independently failed) → WU5C1B2B1 (historical verification-only; independently failed) → WU5C1B2B1A → independent verification → WU5C1C1 (historical verification-only; independently failed) → WU5C1C1A → independent verification → WU5C1C2 (historical verification-only; independently failed) → WU5C1C2A → independent verification → WU5C2A (historical failed) → WU5C2A1 (accepted +70/-3) → WU5C2B (historical untouched oversized/non-accepting) → WU5C2B1 → independent verification → WU5C2B2 → independent verification → WU5D1 → WU5D2 (historical independently failed) → WU5D2A (historical independently failed) → WU5D2A1 (accepted +270/-97) → WU5E1 (historical untouched oversized/nonexecuting) → WU5E1A → independent verification → WU5E1B → independent verification → WU5E2 → WU6A → independent verification → WU6B → independent verification → WU7 → WU8 → WU9 → WU10; each accepted unit ≤400 authored lines |
| Delivery strategy | ask-on-risk |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

Standing `stacked-to-main` split authorization covers WU5C1B2A through WU5C1B2B1A, the reset-authorized C chain, WU5C2A1 → WU5C2B1 → WU5C2B2, the user-authorized WU5D2A → accepted WU5D2A1 correction chain, the user-authorized WU5E1A → WU5E1B → failed WU5E1B1 → WU5E1B2 correction chain, and the explicitly user-authorized WU6A → WU6B split, so no delivery decision is pending. Checked acquisition B2/B2B/B2B1, C1, C2, enumeration A, D2, D2A, E1B wiring, and E1B1 are failed/not accepted historical evidence and satisfy no dependency. WU5C2A1 is accepted after corrected +70/-3 accounting and verifier PASS. The original WU5C2B and WU5E1 are untouched, oversized, blocked, and non-accepting/nonexecuting. WU5E1A is accepted at exact +400/-0=400; WU5E1B2 is next and depends on accepted E1A, while E2 remains blocked on independently accepted E1B2. Generated `go.sum` remains excluded from authored accounting.

### Per-work-unit forecast

| Work unit | Scope | Additions | Deletions | Authored changed lines |
| --- | --- | ---: | ---: | ---: |
| WU1 | Module bootstrap and core named values | 220–260 | 0 | 220–260 |
| WU2A | Estimates and observable domain facts | 240–310 | 0–10 | 240–320 |
| WU2B | Scan policy and immutable snapshot | 250–330 | 0–10 | 250–340 |
| WU3 | Deterministic finalizer and identity accounting | 250–310 | 0–5 | 250–315 |
| WU4A | Built-ins and `HomeResolver` | 190–275 | 0–5 | 190–280 |
| WU4B | Darwin/non-Darwin metadata adapters (historical attempted implementation) | 225–335 | 0–5 | 225–340 |
| WU4C | Descriptor-backed metadata safety remediation (historical failed attempt) | 275–360 | 0–30 | 275–390 |
| WU4D1 | Resolver and accepted descriptor facts | 220–290 | 10–35 | 230–325 |
| WU4D2 | Typed-error and descriptor lifecycle hardening (checked candidate; D2 verification failed) | 170–240 | 5–30 | 175–270 |
| WU4E | Reachable-error ownership correction | 110–180 | 40–100 | 150–280 |
| WU5C1A | Scan-owned descriptor contract only (checked candidate; C1A verification failed) | 130–190 | 10–30 | 140–220 |
| WU5C1A2 | Contract constructor, limit semantics, and ownership correction | 90–150 | 10–40 | 100–190 |
| WU5C1B1 | Darwin core trusted-root acquisition and essential acceptance gates (checked candidate; independent B1 verification failed) | 250–320 | 0–30 | 250–350 |
| WU5C1B1A | Reachable acquisition handoff-close correction | 70–120 | 5–25 | 75–145 |
| WU5C1B2 | Acquisition cancellation, limits, and lifecycle candidate (checked; independently failed) | 222 | 86 | 308 |
| WU5C1B2A | Reachable cancellation and acquisition capability-limit correction | 140–220 | 10–40 | 150–260 |
| WU5C1B2B | Lifecycle matrix test evidence (checked; verification-only and independently failed) | 266 | 1 | 267 |
| WU5C1B2B1 | Missing primary × owned-current-close Cartesian verification (checked; verification-only and independently failed) | 60–140 | 0 | 60–140 |
| WU5C1B2B1A | Secondary-close class verification correction | 10–40 | 0 | 10–40 |
| WU5C1C1 | Real Darwin descriptor fixtures and separate 7→9→7 poison simulation (checked; independently failed) | 233 | 0 | 233 |
| WU5C1C1A | Replacement-identity and consumed-device-trace verification correction | 20–80 | 0 | 20–80 |
| WU5C1C2 | Acquisition checkpoint×limit and remaining lifecycle matrices (checked; verification-only and independently failed) | 116 | 0 | 116 |
| WU5C1C2A | Trace/count and exhaustive lifecycle-inventory verification correction | 20–80 | 0 | 20–80 |
| WU5C2A | Core descriptor-owned enumeration (checked historical candidate; independently failed) | 274 | 2 | 276 |
| WU5C2A1 | Accepted directory-record terminator correction (actual) | 70 | 3 | 73 |
| WU5C2B | Historical untouched combined lifecycle/fixture unit; blocked and non-accepting | — | — | 550–810 |
| WU5C2B1 | Core DFS, FD budget, lifecycle, precedence, and safe siblings | 240–360 | 20 | 260–380 |
| WU5C2B2 | Adversarial real descriptor fixtures, verification-first | 180–300 | 0 | 180–300 |
| WU5D1 | Serial five-root scanner consumer and transitional observations | 240–320 | 0–20 | 240–340 |
| WU5D2 | Directory-fact mapping candidate (checked historical; independently failed) | 257 | 1 | 258 |
| WU5D2A | Production fact-port and enumerated/opened kind correction (checked historical; independently failed) | 196 | 43 | 239 |
| WU5D2A1 | Accepted unified recursive Darwin fact traversal correction (actual) | 270 | 97 | 367 |
| WU5E1 | Historical untouched combined file-pipeline/reopen unit; blocked and nonexecuting | — | — | 600–925 |
| WU5E1A | Accepted scanner-owned bounded file pipeline and opaque contract (actual) | 400 | 0 | 400 |
| WU5E1B | Darwin private-root descriptor-relative file reopen (historical wiring-incomplete candidate) | 280–390 | 0 | 280–390 |
| WU5E1B1 | Scanner FilePort wiring correction (checked historical; independently failed) | 180–320 | 0–40 | 180–360 |
| WU5E1B2 | Scanner-owned accepted-result retention correction | 120–220 | 0–30 | 120–250 |
| WU5E2 | Pipeline cancellation integration, deterministic aggregation, and hard links | 230–300 | 10–30 | 240–330 |
| WU6A | Policy/limits contract, defaults, and entry/path enforcement | 210–285 | 5–20 | 215–305 |
| WU6B | Cancellation/status precedence, shutdown, and completion order | 245–300 | 0–5 | 245–305 |
| WU7 | Deterministic JSON/text reports and goldens | 260–320 | 0–5 | 260–325 |
| WU8 | Minimal read-only Bubble Tea viewer | 230–290 | 0–5 | 230–295 |
| WU9 | Cobra orchestration, streams, and exit classes | 270–340 | 0–5 | 270–345 |
| WU10 | Production entry point and final acceptance | 50–70 | 0–5 | 50–75 |
| **Total** | | **7,205–10,045** | **223–758** | **7,428–10,803** |

**Exact task count:** 170 checkbox rows: 167 implementation-owned = 135 checked + 32 unchecked; 3 parent-owned = 0 checked + 3 unchecked; 135 checked and 35 unchecked overall. Every row has exactly one terminal ownership marker. WU5C2A's four checked rows remain historical failed evidence; A1's four checked rows carry accepted +70/-3 accounting; the four original B rows remain explicitly unchecked historical oversized/non-accepting context, followed by four unchecked B1 rows and four unchecked B2 rows. WU5D2's four checked rows remain historical independently failed/non-accepting evidence. WU5D2A's four checked rows remain historical independently failed/non-accepting evidence at +196/-43=239; WU5D2A1's four checked rows carry accepted +270/-97=367 accounting. The original four WU5E1 rows remain unchecked historical nonexecuting context. WU5E1A's four checked rows are accepted at exact +400/-0=400; WU5E1B's four rows and WU5E1B1's four rows are historical failed evidence, WU5E1B2's four rows are next, and WU5E2 waits for independently accepted E1B2.

## Execution Rules

- Use one writer and strict sequence through WU5C1B2 (failed history) → WU5C1B2A → WU5C1B2B (failed verification-only history) → WU5C1B2B1 (failed verification-only history) → WU5C1B2B1A → independent verification → WU5C1C1 (failed verification-only history) → WU5C1C1A → independent verification → WU5C1C2 (failed verification-only history) → WU5C1C2A → independent verification → WU5C2A (failed history) → WU5C2A1 (accepted) → WU5C2B (historical untouched oversized/non-accepting) → WU5C2B1 → independent verification → WU5C2B2 → independent verification, then preserve WU5D1 → failed historical WU5D2 → failed historical WU5D2A → accepted WU5D2A1 → historical nonexecuting WU5E1 → accepted WU5E1A → WU5E1B → independent verification → WU5E2 → WU6A → independent verification → WU6B → independent verification → WU7–WU10 order; never parallelize writes.
- Advance each work unit through SDD apply/verify authority and its focused evidence; work-unit progression and SDD completion do not launch review actors or review transactions.
- WU5C1C1A, WU5C1C2, and WU5C1C2A are verification-only units with no fabricated RED and no authorized production edit; C1A and C2A use VERIFY → REFACTOR, while historical failed C2 used VERIFY → TRIANGULATE → REFACTOR. A correct PASS on unchanged production is accepted verification. Any correct failure stops immediately and requires a separately planned, independently bounded strict RED → GREEN behavior-correction unit before any production edit. Record exact hashes, additions+deletions, runtime N/A, rollback, and production byte-identity evidence for each ≤400 slice. Failed B2, B2B, B2B1, C1, C2, D2, D2A, and the failed historical WU5C1C candidate cannot satisfy a dependency.
- Because the implementation files are untracked, account WU2A against the last accepted WU1 file hashes recorded in `openspec/changes/read-only-scan-foundation/apply-progress.md`: `internal/core/types.go` `19ccd112da34dfb6a34f702dae0afc7c4d4f037bc867c3151728c8dbbb5323a4` and `internal/core/types_test.go` `e79a225ca2178da8b050217093823070b207d0675b93c3ada998fbd54a456431`; `internal/core/snapshot.go` and `snapshot_test.go` were absent at that accepted boundary. The current 497-addition interrupted attempt is not an accepted baseline: removal or deferral of its WU2B-only lines is candidate normalization, while the accepted WU2A receipt counts the final authored delta from WU1 hashes. Account WU2B from the accepted WU2A hashes.
- Use an apply receipt or another file-level accounting mechanism when Git metadata is absent. Do not require commits for rollback or verification.
- Ordinary review occurs only if separately and explicitly requested through a bound transaction. Lifecycle gates are not implementation tasks and remain absent unless the user later requests commit, push, PR, or release.
- The repository has no Go module before WU1. WU1 must create a compiling module boundary; every later work unit must finish with its focused package tests passing.
- Use `t.TempDir()` and injected home, descriptor-walker, metadata, terminal, renderer, and stream seams in automated tests. No test may call the production home resolver or inspect the real home.
- Keep all implementation read-only: no cleanup, deletion, Trash, selection, plan, confirmation, shell/executable invocation, network, privilege, telemetry, daemon, arbitrary root, external-volume traversal, or product-tool adapter.
- Use `gofmt`; `goimports` is not configured in the current repository and must not be added solely for this change.

## Normative Requirement Coverage

| Normative requirement | Implementation work | Verification evidence |
| --- | --- | --- |
| Fixed Built-In Scan Coverage | WU4A fixed table/resolver; WU5D1 serial five-root acquisition; failed WU5D2/D2A; WU5D2A1 unified production fact traversal; WU9 no-args CLI validation | WU4A/D1 prove canonical inputs and order; D2A1 proves all five acquired production-shaped roots invoke the same synchronous public recursive fact port exactly once; poison-scanner path rejection proves no platform call before validation. |
| Strict Read-Only and Local Scope | WU4E; WU5C1A/A2; WU5C1B1/B1A; failed B2; B2A; failed verification-only B2B/B2B1; B2B1A; failed verification-only C1; C1A; failed verification-only C2; C2A; failed WU5C2A; WU5C2A1–WU5D1; failed WU5D2/D2A; WU5D2A1–WU5E2; WU7–WU9 | B2A proves reachable stop/no-deeper-work; B2B1A proves typed current-close class; C1A proves retained-descriptor identity and exact consumed mount trace; C2A proves checkpoint/lifecycle coverage; A1 proves malformed unterminated records are rejected before any child open; D2A1 proves one recursive descriptor engine supplies scanner facts without content reads, raw FDs, duplicate traversal, or ownership leaks. |
| Explicit Root Observation States | WU1/WU2 statuses and outcome; WU5D1; failed WU5D2/D2A; WU5D2A1 recursive root/descendant/event mapping; WU5E2/WU6B precedence | Production-shaped and scanner fixtures cover every root state, nested accepted facts, changed/disappeared/boundary events, stable symlink/special semantics, and `cancelled > partial > boundary_limited` with retained warnings. |
| Bounded Traversal | WU5C1A/A2 contract; WU5C1B1/B1A core; WU5C1B2 failed history; WU5C1B2A reachable cancellation/acquisition capability limits; WU5C1B2B/B2B1 failed verification evidence; WU5C1B2B1A Cartesian class verification; failed verification-only C1; WU5C1C1A; failed verification-only C2; WU5C1C2A; failed WU5C2A; WU5C2A1/WU5C2B1/WU5C2B2; failed WU5D2/D2A; WU5D2A1/WU5E1A/WU5E1B | A1 validates terminated records; B1/B2 prove bounded descriptor ownership and replacement safety; D2A1 uses that same recursive engine for scanner facts while preserving DFS depth, FD capacity, LIFO closes, error precedence, replacement safety, and no duplicate traversal. |
| Explainable Findings and Honest Estimates | WU1/WU2A values; WU3 grouping/accounting; WU5E1B accepted file facts; WU5E2 finalizer integration; WU7/WU8 language | Constructor, finalizer, worker, report, and viewer tests prove area/path/reason/`manual_review`/basis/warnings, explicit unknown allocation, caveats, and no safety or reclaim guarantees. |
| Repeated Filesystem Identity Accounting | WU3 post-sort attribution; WU5E1B accepted final-descriptor identities; WU5E2 hard-link finalization | Reordered worker and hard-link cases prove changed identities contribute zero and canonical post-sort attribution is scheduling-independent. |
| Versioned Shared Snapshot Contract | WU2 immutable snapshot; failed WU5D2/D2A; WU5D2A1/WU5E2 scanner integration; WU7–WU9 snapshot-only consumers | Snapshot/accessor tests and production-shaped scan counters prove one synchronous recursive fact traversal per acquired root, five roots, derived completeness, no duplicate traversal or rescan, and no presentation-specific fact model. |
| Deterministic Snapshot and Serialization Order | WU2/WU3 canonical facts; failed WU5C2A plus corrective WU5C2A1 record acceptance; failed WU5D2/D2A plus WU5D2A1 unified DFS fact correction; WU5E2 completion permutations; WU7 DTOs | A1 first rejects malformed records without child opens; D2A1 emits separate enumerated/opened evidence in one deterministic lexical DFS without duplicate paths; worker/finalizer permutations and goldens prove stable output. |
| Equivalent Text and JSON Reports | WU7 shared projections; WU9 stream selection | `TestReportEquivalence` compares schema, outcome, roots, findings, risk, estimate bases/completeness, and warnings for complete, partial, and cancelled snapshots. |
| Minimal Read-Only Terminal Viewer | WU8 model/view/run; WU9 terminal validation | Direct model tests cover navigation, details, quit, and all outcomes while forbidding action affordances. |
| Isolated Errors and Global Failures | WU5C1A/A2; acquisition correction chain; failed WU5C2A; WU5C2A1/WU5C2B1/WU5C2B2; WU5D1; failed WU5D2/D2A; WU5D2A1–WU5E1B; WU9 | A1 rejects malformed records before child open; WU5C2B1 preserves lifecycle precedence; WU5C2B2 proves replacement safety; D2A1 emits changed/disappeared/boundary events synchronously while stable symlink/special and existing error semantics continue safe siblings and later roots. |
| Cancellation Produces an Incomplete Snapshot | WU2 outcome; WU5E2 pipeline stop/drain/join; WU6A limits; WU6B cancellation/precedence; WU8/WU9 presentation/exits | Channel-synchronized race-tested cases prove no new work, drained accepted results, joined workers, completed/active/unstarted states, and incomplete estimates. |
| Stable Streams and Scan Exit Classes | WU9 render-before-write and exits; WU10 signal context | CLI tests cover stdout/stderr purity and codes 0/1/2/3/4/130, including parseable partial/cancelled JSON and cancellation precedence. |
| Disposable and Deterministic Verification | WU5C1A/A2; acquisition correction chain; failed WU5C2A; WU5C2A1/WU5C2B1/WU5C2B2; WU5D1; failed WU5D2/D2A; WU5D2A1/WU5E1A/WU5E1B/WU5E2; WU10 | A1 uses deterministic record/syscall seams; WU5C2B2 owns disposable rename/replacement fixtures; D2A1 adds the exact missing scan/Darwin tests for nested synchronous facts, five traversals, ownership close-once, kind/identity pairs, no duplicate traversal, and no raw FD/content/goroutine work. |

## Implementation Work Units

### WU1 — Bootstrap the Module and Core Named Values

**Depends on:** approved tasks plus the pre-apply delivery and module-import identity decisions.  
**Allowed edit surfaces:** `go.mod`, generated `go.sum`, `internal/core/types.go`, `internal/core/types_test.go`.  
**Start:** no `go.mod` or Go package exists.  
**Finish:** the root Go 1.24 module and `internal/core` compile with validated named values; use module identity `osdycleaner` from `openspec/config.yaml` unless the pre-apply gate records a canonical hosted import path.  
**Focused command:** `go test ./internal/core -run '^(TestParseAreaID|TestParseRootStatus|TestParseRootReasonCode|TestParseWarningCode|TestAllocation|TestFilesystemIdentity)$' -count=1`.  
**Runtime harness evidence:** N/A — no executable or scan boundary exists yet.  
**Rollback boundary:** remove `go.mod`, any generated `go.sum`, `internal/core/types.go`, and `internal/core/types_test.go`; this returns to the documented bootstrap-era repository without touching other artifacts.

- [x] **WU1 RED:** Create `go.mod` and table-driven `internal/core/types_test.go` cases for all specified area IDs/ranks, statuses, reasons, outcomes, `manual_review`, warning codes/messages, filesystem identity, known/unknown allocation, estimate basis, and completeness; run the focused command and record a non-zero compile/test result caused only by the missing core API. <!-- sdd-owner: implementation -->
- [x] **WU1 GREEN:** Implement the minimum validated constants, parsers, fixed `AreaRank` switch, optional identity/allocation values, and stable warning messages in `internal/core/types.go`; rerun the focused command and expect PASS for every named valid case. <!-- sdd-owner: implementation -->
- [x] **WU1 TRIANGULATE:** Add invalid strings, zero/overflow-like values, unknown allocation, and identity-unavailable table rows in `internal/core/types_test.go`; rerun the focused command and expect rejection without panic or logical-size fallback. <!-- sdd-owner: implementation -->
- [x] **WU1 REFACTOR:** Run `gofmt -w internal/core/types.go internal/core/types_test.go && go mod tidy && go test ./internal/core -count=1`; expect a compiling package and PASS, account for generated `go.sum` if created, and stop for review-load handling if authored lines exceed 400. <!-- sdd-owner: implementation -->

### WU2A — Add Estimates and Observable Domain Facts

**Forecast:** 240–310 additions, 0–10 deletions, 240–320 authored changed lines.  
**Depends on:** accepted WU1 hashes and the interrupted WU2 evidence recorded in `openspec/changes/read-only-scan-foundation/apply-progress.md`.  
**Allowed edit surfaces:** `internal/core/types.go`, `internal/core/types_test.go`, `internal/core/snapshot.go`, `internal/core/snapshot_test.go`; edits in the latter two files are limited to estimates/finding/root/warning behavior and bounded candidate normalization that removes or defers WU2B-only APIs/tests.  
**Start:** the repository contains the incomplete 497-addition WU2 candidate, but accepted accounting starts from WU1 hashes; WU2A may remove/defer `ScanPolicy`, `Snapshot`, aggregate/outcome implementation, `TestNewSnapshot`, `TestSnapshotAccessors`, and their WU2B-only helpers so the WU2A boundary compiles without those APIs.  
**Finish:** `ValueEstimate`, `Estimate`, `Accounting`, `Finding`, `RootObservation`, and `Warning` have validated constructors/accessors and immutable-by-convention copy behavior, while WU2B APIs are absent.  
**Focused command:** `go test ./internal/core -run '^(TestNewEstimate|TestNewFinding|TestNewRootObservation|TestWarningCopyBehavior)$' -count=1`.  
**Runtime harness evidence:** N/A — these domain values have no executable or scanner boundary.  
**Rollback boundary:** restore the accepted WU1 versions/hashes of `internal/core/types.go` and `types_test.go`, and remove WU2A-only finding/root/warning portions of `snapshot.go` and `snapshot_test.go`; this removes the complete WU2A fact boundary without touching WU1. Candidate normalization of never-accepted WU2B lines needs no separate rollback promise.

- [x] **WU2A RED:** Reuse and cite the genuine combined RED already recorded in `openspec/changes/read-only-scan-foundation/apply-progress.md`, confirming that its missing `NewEstimate`, `NewValueEstimate`, `NewAccounting`, and `NewFinding` failures cover the WU2A API subset; preserve the tests-first provenance while narrowing `internal/core/snapshot_test.go` to `TestNewEstimate`, `TestNewFinding`, `TestNewRootObservation`, and an explicit `TestWarningCopyBehavior`. <!-- sdd-owner: implementation -->
- [x] **WU2A GREEN:** Reconcile the interrupted candidate into a compiling WU2A-only boundary in `internal/core/types.go`, `snapshot.go`, and `snapshot_test.go`: retain the already GREEN estimate/accounting/finding/root/warning implementation and evidence, remove/defer all WU2B-only policy/snapshot/aggregate code and `TestNewSnapshot`/`TestSnapshotAccessors` helpers, run the focused command, and record PASS. <!-- sdd-owner: implementation -->
- [x] **WU2A TRIANGULATE:** Add focused table rows for invalid estimate basis/completeness, inconsistent accounting, invalid area/display path/reason/risk, every valid and invalid status/reason pairing, stable code-owned warning messages without raw OS errors, sorted/deduplicated warning codes, optional related paths, and input/accessor slice-copy mutation; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU2A REFACTOR:** Run `gofmt -w internal/core/types.go internal/core/types_test.go internal/core/snapshot.go internal/core/snapshot_test.go && go test ./internal/core -count=1`; expect PASS with WU2B APIs absent, runtime evidence recorded as N/A, final hashes recorded, and authored additions plus deletions measured from the accepted WU1 hashes at no more than 400. <!-- sdd-owner: implementation -->

### WU2B — Add Scan Policy and the Immutable Snapshot

**Forecast:** 250–330 additions, 0–10 deletions, 250–340 authored changed lines.  
**Depends on:** accepted WU2A, with `ScanPolicy`, `Snapshot`, aggregate/outcome derivation, and WU2B tests absent.  
**Allowed edit surfaces:** `internal/core/snapshot.go`, `internal/core/snapshot_test.go`; `internal/core/types.go` and `internal/core/types_test.go` only if a snapshot-specific invariant requires a minimal adjacent value change.  
**Start:** the bounded WU2A candidate compiles and exposes estimates, findings, roots, and warnings only; the interrupted attempt's prior snapshot GREEN is historical context, not WU2B RED evidence or an accepted baseline.  
**Finish:** `ScanPolicy` and schema-v1 `Snapshot` validate exactly five unique canonical roots, derive outcome/completeness/aggregate estimates, canonicalize collections, deep-copy accessors, represent all-missing complete zero and mixed-status precedence, and retain no runtime-varying metadata.  
**Focused command:** `go test ./internal/core -run '^(TestNewScanPolicy|TestNewSnapshot|TestSnapshotAccessors|TestSnapshotOutcomes)$' -count=1`.  
**Runtime harness evidence:** N/A — the immutable snapshot domain has no runnable scanner boundary.  
**Rollback boundary:** remove only WU2B policy/snapshot/aggregate implementation and its `TestNewScanPolicy`, `TestNewSnapshot`, `TestSnapshotAccessors`, `TestSnapshotOutcomes`, and helpers from the allowed files; the accepted WU2A estimate/finding/root/warning package remains compiling.

- [x] **WU2B RED:** Beginning from accepted WU2A with WU2B APIs absent, add table-driven `internal/core/snapshot_test.go` tests first for policy validation, exactly five unique canonical roots, fixed schema/rule-set/policy facts, canonical collection order, derived aggregate/outcome/completeness, and deep-copy accessors; run the focused command and capture a fresh genuine non-zero RED caused only by missing WU2B APIs. <!-- sdd-owner: implementation -->
- [x] **WU2B GREEN:** Implement the minimum unexported immutable-by-convention `ScanPolicy` and `Snapshot` constructors/accessors in `internal/core/snapshot.go`, including exact five-root validation, canonicalization, aggregate derivation, all-missing complete zero, and caller-independent outcome selection; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU2B TRIANGULATE:** Add policy/outcome rows for duplicate/missing/out-of-order roots, mixed scanned/missing/inaccessible/skipped/boundary-limited/partial states, `cancelled > partial > boundary_limited` precedence, incomplete partial/cancelled estimates, explicit unknown allocation, sorted/deduplicated findings/warnings/codes, no runtime-varying wall-clock/host/user/random metadata, and attempted input/accessor mutation; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU2B REFACTOR:** Run `gofmt -w internal/core/types.go internal/core/types_test.go internal/core/snapshot.go internal/core/snapshot_test.go && go test ./internal/core -count=1`; expect PASS, record runtime evidence as N/A and the WU2B rollback boundary, and measure authored additions plus deletions from accepted WU2A hashes at no more than 400 before advancing. <!-- sdd-owner: implementation -->

### WU3 — Finalize Deterministic Findings and Identity Accounting

**Depends on:** WU2B.  
**Allowed edit surfaces:** `internal/scan/finalize.go`, `internal/scan/finalize_test.go`.  
**Start:** WU2B core constructors enforce snapshot invariants.  
**Finish:** raw observations are canonically sorted, identity-accounted across areas, grouped by immediate root child, and converted to deterministic facts.  
**Focused command:** `go test ./internal/scan -run '^(TestFinalizeCanonicalOrdering|TestFinalizeRepeatedIdentity|TestFinalizeUnknownMetadata|TestFinalizeFindingGroups)$' -count=1`.  
**Runtime harness evidence:** N/A — finalization is a package boundary, not an executable scan.  
**Rollback boundary:** remove `internal/scan/finalize.go` and `internal/scan/finalize_test.go`; the WU2B core package remains coherent.

- [x] **WU3 RED:** Create controlled raw-observation tests in `internal/scan/finalize_test.go` for reordered discovery, area/path/reason sorting, same-identity hard links within and across areas, immediate-child grouping, empty roots, and unknown identity/allocation; run the focused command and record the expected missing-finalizer failures. <!-- sdd-owner: implementation -->
- [x] **WU3 GREEN:** Implement canonical sort-before-accounting, the cross-root identity table, zero incremental bytes for later paths, stable related-path warnings, regular-file-only totals, immediate-child finding groups, and sorted/deduplicated warning facts in `internal/scan/finalize.go`; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU3 TRIANGULATE:** Add permutation rows proving worker/discovery timing cannot choose attribution, unknown identity preserves measured bytes while disabling complete deduplication, unknown allocation never falls back, APFS/CoreSimulator caveats appear where required, and repeated identity is never described as duplicate-file analysis; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU3 REFACTOR:** Run `gofmt -w internal/scan/finalize.go internal/scan/finalize_test.go && go test ./internal/scan -count=1`; expect PASS with no map iteration used to emit snapshot facts and no per-file observations retained after grouping. <!-- sdd-owner: implementation -->

### WU4A — Define Built-Ins and the Injected Home Resolver

**Forecast:** 190–275 additions, 0–5 deletions, 190–280 authored changed lines.  
**Depends on:** accepted WU3B.  
**Allowed edit surfaces:** `internal/scan/builtins.go`, `internal/scan/builtins_test.go`.  
**Start:** deterministic finalization compiles, but no built-in root definitions or home-resolution boundary exists.  
**Finish:** a syscall-free injected `HomeResolver` boundary validates an absolute, inspectable home and returns exactly five immutable definitions in canonical order: `npm-cache` / npm cache / `~/.npm` / `.npm` / `built_in_npm_cache_entry`; `homebrew-cache` / Homebrew cache / `~/Library/Caches/Homebrew` / `Library/Caches/Homebrew` / `built_in_homebrew_cache_entry`; `gradle-caches` / Gradle caches / `~/.gradle/caches` / `.gradle/caches` / `built_in_gradle_cache_entry`; `xcode-derived-data` / Xcode DerivedData / `~/Library/Developer/Xcode/DerivedData` / `Library/Developer/Xcode/DerivedData` / `built_in_xcode_derived_data_entry`; and `core-simulator` / CoreSimulator / `~/Library/Developer/CoreSimulator` / `Library/Developer/CoreSimulator` / `built_in_core_simulator_entry`.  
**Focused command:** `go test ./internal/scan -run '^(TestResolveBuiltins|TestResolveBuiltinsHomeValidation|TestResolveBuiltinsPoisonCases)$' -count=1`.  
**Runtime harness evidence:** N/A — WU4A is a pure injected resolver/table boundary and performs no filesystem syscall or traversal.  
**Rollback boundary:** remove `internal/scan/builtins.go` and `internal/scan/builtins_test.go`; accepted WU3 finalization remains compiling and no metadata adapter is affected.

- [x] **WU4A RED:** In `internal/scan/builtins_test.go`, add tests first for the exact five IDs, display names, `~` display paths, relative suffixes, finding reasons, and canonical order, plus injected-resolver failures for empty, invalid, and non-absolute homes and a poison inspectability seam that proves rejected input cannot fall back to the real home or any syscall; run the WU4A focused command and capture a non-zero failure caused only by missing WU4A APIs. <!-- sdd-owner: implementation -->
- [x] **WU4A GREEN:** In `internal/scan/builtins.go`, implement the minimum fixed table and injected `HomeResolver`/home-inspectability boundary, validate that the returned home is absolute and inspectable, lexically join only the five fixed suffixes, and return copied canonical definitions without `os.UserHomeDir`, `Lstat`, `Statfs`, `EvalSymlinks`, traversal, overrides, shell commands, or other syscalls; rerun the WU4A focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4A TRIANGULATE:** Extend `internal/scan/builtins_test.go` with table-copy mutation, resolver-error, whitespace/relative/poison home, duplicate/extra-root impossibility, and path-cleaning edge cases; rerun the WU4A focused command and expect exact canonical facts, no real-home access, no injected-inspector call after invalid/non-absolute input, and no syscall or traversal. <!-- sdd-owner: implementation -->
- [x] **WU4A REFACTOR:** Run `gofmt -w internal/scan/builtins.go internal/scan/builtins_test.go && go test ./internal/scan -run '^(TestResolveBuiltins|TestResolveBuiltinsHomeValidation|TestResolveBuiltinsPoisonCases)$' -count=1 && go test ./internal/scan -count=1`; expect PASS, record runtime evidence as N/A and the two-file rollback boundary, and measure WU4A at no more than 400 authored changed lines before authorizing WU4B. <!-- sdd-owner: implementation -->

### WU4B — Add Darwin and Non-Darwin Metadata Adapters

**Forecast:** 225–335 additions, 0–5 deletions, 225–340 authored changed lines.  
**Depends on:** accepted WU4A.  
**Allowed edit surfaces:** `internal/platform/macos/metadata_darwin.go`, `internal/platform/macos/metadata_other.go`, `internal/platform/macos/metadata_darwin_test.go`.  
**Start:** accepted WU4A supplies fixed roots and injected home facts, but no production metadata adapter or unsupported-environment constructor exists.  
**Finish:** Darwin uses only `os.Lstat`, `syscall.Stat_t`, and `syscall.Statfs` to report typed non-following metadata/local-volume facts; non-Darwin construction fails with a typed unsupported-environment error before home resolution or traversal; no adapter traverses directories.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestDarwinMetadataDisposable|TestDarwinMetadataClassification|TestDarwinMetadataUncertainty)$' -count=1`.  
**Runtime harness evidence:** N/A — disposable adapter tests are the bounded metadata-call harness, and no scanner or CLI traversal boundary exists.  
**Rollback boundary:** remove `internal/platform/macos/metadata_darwin.go`, `metadata_other.go`, and `metadata_darwin_test.go`; accepted WU4A built-ins remain intact and no traversal code is removed.

- [x] **WU4B RED:** In Darwin-tagged `internal/platform/macos/metadata_darwin_test.go`, add disposable `t.TempDir()` file/directory/hard-link tests and controlled adapter-block cases for missing paths, symlinks, non-directories, device mismatch, non-local filesystems, inaccessible metadata, unknown identity/allocation, negative or overflowing block counts, and link counts; run the WU4B focused command and capture a non-zero failure caused only by missing metadata APIs. <!-- sdd-owner: implementation -->
- [x] **WU4B GREEN:** Implement `internal/platform/macos/metadata_darwin.go` with non-following `os.Lstat` plus checked `syscall.Stat_t` extraction for mode/device/inode/link count/logical size/allocation and `syscall.Statfs` local-volume facts, and implement `metadata_other.go` with a typed unsupported-environment constructor that fails before home resolution; rerun the WU4B focused command and expect PASS without traversal or invented identity/allocation bytes. <!-- sdd-owner: implementation -->
- [x] **WU4B TRIANGULATE:** Extend `metadata_darwin_test.go` to prove hard links share identity while preserving link counts, disposable fixtures retain content/mode/mtime, missing/symlink/non-directory/device/local/inaccessible cases remain typed, unavailable identity/allocation stays unknown, and negative/overflowing blocks never wrap or fall back to logical size; rerun the WU4B focused command and expect PASS with no directory walk or target following. <!-- sdd-owner: implementation -->
- [x] **WU4B REFACTOR:** Run `gofmt -w internal/platform/macos/metadata_darwin.go internal/platform/macos/metadata_other.go internal/platform/macos/metadata_darwin_test.go && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /dev/null ./internal/platform/macos`; expect Darwin package PASS plus successful typed non-Darwin compilation, record runtime evidence as N/A and the three-file rollback boundary, and measure WU4B independently at no more than 400 authored changed lines before authorizing WU5. <!-- sdd-owner: implementation -->

### WU4C — Remediate Metadata Safety with Revalidated Non-Following Descriptors (Historical Failed Attempt)

**Forecast:** 275–360 additions, 0–30 deletions, 275–390 authored changed lines; this was an independent WU4C budget and does not merge with WU4B, WU4D1, or WU4D2 accounting.  
**Depends on:** accepted WU4A and the preserved checked WU4B attempted surfaces; WU4B's receipt remains failed historical evidence and is not an acceptance claim.  
**Allowed edit surfaces:** `internal/platform/macos/metadata_darwin.go`, `internal/platform/macos/metadata_other.go`, `internal/platform/macos/metadata_darwin_test.go`.  
**Start:** the WU4B files exist, but pathname-only metadata acceptance and its evidence are not trusted against races or target substitution.  
**Finish:** this attempted final-component-only descriptor boundary is not accepted; failed evidence `sha256:5d38e230...` and its non-reproducible predecessor/baseline claims are historical superseded evidence, and accepted WU4D1 plus WU4D2 must replace the unsafe acquisition before traversal.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestDarwinMetadataRegularOnlyBytes|TestDarwinMetadataTypedErrors|TestDarwinMetadataFailClosedFields|TestDarwinMetadataNoFollowSwap|TestDarwinMetadataDescriptorLifecycle)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized disposable descriptor/race fixtures are the bounded metadata harness; no scanner or CLI exists.  
**Rollback boundary:** restore exactly the three WU4B file hashes recorded in the historical WU4B receipt; accepted WU4A and earlier packages remain unchanged, and neither WU4D1, WU4D2, nor WU5 may be bypassed.

- [x] **WU4C RED:** In Darwin-tagged `internal/platform/macos/metadata_darwin_test.go`, add tests first proving directories and FIFO/socket/device-like special entries report zero logical bytes with unknown allocation; a concrete adapter-owned typed path error supports both `errors.Is` against stable sentinel classes and `errors.As` for path/operation context; zero device/inode identity, zero link count, negative logical size, negative block count, non-`syscall.Stat_t`, and `statfs` failure fail closed; and a synchronized adversarial `Lstat`→symlink swap never follows or accepts target metadata; run the WU4C focused command and capture a non-zero failure caused only by the missing safety behavior. <!-- sdd-owner: implementation -->
- [x] **WU4C GREEN:** In `internal/platform/macos/metadata_darwin.go` and `metadata_other.go`, implement the minimum fail-closed adapter: acquire each Darwin path with `O_NOFOLLOW` plus read-only, close-on-exec, and nonblocking safe flags; obtain accepted facts only through descriptor `Fstat`/`Fstatfs`; revalidate descriptor identity and type against the preceding `Lstat`; expose logical/allocation bytes only for regular files; reject zero identity/link count and negative logical/blocks without invention; and return concrete typed classified errors that wrap stable sentinel classes while preserving typed unsupported construction on non-Darwin; rerun the WU4C focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4C TRIANGULATE:** Extend `internal/platform/macos/metadata_darwin_test.go` with synchronized open/fstat/fstatfs/close seams proving descriptor close on every success and failure path, fail-closed race/type/identity changes and symlink swaps, no FIFO/device blocking, allocation overflow or unknown remains unknown without logical fallback, and Linux builds against the concrete typed unsupported error; rerun the focused command and `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test`, expecting PASS without target facts, leaked descriptors, blocking, wrapping, or invented bytes. <!-- sdd-owner: implementation -->
- [x] **WU4C REFACTOR:** Run `gofmt -w internal/platform/macos/metadata_darwin.go internal/platform/macos/metadata_other.go internal/platform/macos/metadata_darwin_test.go && go test ./internal/platform/macos -run '^(TestDarwinMetadataRegularOnlyBytes|TestDarwinMetadataTypedErrors|TestDarwinMetadataFailClosedFields|TestDarwinMetadataNoFollowSwap|TestDarwinMetadataDescriptorLifecycle)$' -count=1 && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos/metadata_darwin.go internal/platform/macos/metadata_other.go internal/platform/macos/metadata_darwin_test.go)"`; expect every focused/platform/Linux cross-build/full/vet/format diagnostic to pass, record runtime evidence as N/A, exact file hashes and rollback boundary, and independently measure WU4C additions plus deletions at no more than 400 before authorizing WU5. <!-- sdd-owner: implementation -->

### WU4D1 — Establish the Component-Wise Resolver and Accepted Facts

**Forecast:** 220–290 additions, 10–35 deletions, 230–325 authored changed lines; independently capped at 400 and accounted from the restored WU4C baseline.  
**Depends on:** accepted WU4A plus the restored WU4C file baseline in `/tmp/osdy-wu4d-baseline-1788066883-49900/`; the original 497-line WU4D candidate and blocked receipt are superseded, not accepted.  
**Allowed edit surfaces:** `internal/platform/macos/metadata_darwin.go`, `internal/platform/macos/metadata_darwin_test.go`; use `metadata_other.go` only for a minimum compiling shared error surface.  
**Start:** product files match the recorded WU4C hashes; no component-wise candidate remains and every original WU4D row was unchecked.  
**Finish:** a compiling, passing, safety-improved Darwin boundary parses only clean absolute components, opens root/intermediate/final descriptors component-wise with exact safe flags, closes the complete descriptor chain, never follows ancestor or final symlinks, and accepts only final-descriptor `Fstat`/`Fstatfs` facts. It exposes bytes only for regular files and revalidates type, identity, device, and locality. Error classification is limited to the stable minimum needed by this resolver boundary; exhaustive concrete typing and lifecycle precedence remain WU4D2 work.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestDarwinMetadataComponentParsing|TestDarwinMetadataOpenatFlags|TestDarwinMetadataDescriptorChain|TestDarwinMetadataNoFollowComponents|TestDarwinMetadataAcceptedFacts|TestDarwinMetadataRegularOnlyBytes|TestDarwinMetadataRevalidation|TestDarwinMetadataResolverErrors)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized disposable resolver/descriptor fixtures are the bounded harness; no scanner or CLI exists.  
**Rollback boundary:** restore the three baseline metadata files from `/tmp/osdy-wu4d-baseline-1788066883-49900/` and remove only WU4D1-created files; accepted WU4A and earlier packages remain untouched. Keep the baseline directory after rollback or acceptance until WU4D2 is independently verified.

- [x] **WU4D1 RED:** In `internal/platform/macos/metadata_darwin_test.go`, add failing tests first for clean absolute component parsing and rejection of `.`, `..`, empty/repeated/trailing suffix components; exact root/intermediate/final `O_NOFOLLOW|O_CLOEXEC|O_NONBLOCK` flags with `O_DIRECTORY` only where required; root/intermediate/final descriptor open/close counts; ancestor and final symlink no-follow; final-descriptor-only `Fstat`/`Fstatfs` accepted facts; regular-file-only bytes; type/identity/device/local revalidation; and the minimum stable missing/symlink/non-directory/inaccessible resolver error classes; run the WU4D1 focused command and capture a non-zero RED caused only by missing WU4D1 behavior. <!-- sdd-owner: implementation -->
- [x] **WU4D1 GREEN:** In the allowed Darwin metadata files, implement the minimum component-wise resolver anchored at a trusted absolute root descriptor; validate every component, apply the exact safe `openat`-style flags, close every root/intermediate/final descriptor on all currently exercised paths, populate `Metadata` only from final-descriptor `Fstat` and `Fstatfs`, expose bytes only for regular files, and fail closed on type/identity/device/local mismatch without path-based `Statfs`, content reads, traversal, mutation, or target facts; rerun the WU4D1 focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4D1 TRIANGULATE:** Extend the disposable synchronized resolver seams with ancestor/final swaps, exact-flag rejection, directory/FIFO/socket/device-like special types, basic open/fstat/fstatfs errors, and root/intermediate/final descriptor-count permutations; prove redirected or special targets contribute no accepted facts and every opened descriptor closes; rerun the WU4D1 focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4D1 REFACTOR:** Run `gofmt -w internal/platform/macos/metadata_darwin.go internal/platform/macos/metadata_darwin_test.go internal/platform/macos/metadata_other.go && go test ./internal/platform/macos -run '^(TestDarwinMetadataComponentParsing|TestDarwinMetadataOpenatFlags|TestDarwinMetadataDescriptorChain|TestDarwinMetadataNoFollowComponents|TestDarwinMetadataAcceptedFacts|TestDarwinMetadataRegularOnlyBytes|TestDarwinMetadataRevalidation|TestDarwinMetadataResolverErrors)$' -count=1 && go test ./internal/platform/macos -count=1 && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos)"`; expect focused/platform/full/vet/format PASS, verify no path-based `Statfs`, content read, walk/traversal, mutation, unsafe final-only open, or descriptor leak, and record exact results, runtime N/A, file hashes, rollback boundary, and WU4D1 authored additions plus deletions at no more than 400 before authorizing WU4D2. <!-- sdd-owner: implementation -->

### WU4D2 — Harden Typed Errors and Descriptor Lifecycle

**Forecast:** 170–240 additions, 5–30 deletions, 175–270 authored changed lines; independently capped at 400 and accounted from accepted WU4D1.  
**Depends on:** independently accepted WU4D1 with focused/full checks passing and exact file hashes recorded.  
**Allowed edit surfaces:** `internal/platform/macos/metadata_darwin.go`, `internal/platform/macos/metadata_other.go`, `internal/platform/macos/metadata_darwin_test.go`, `internal/platform/macos/metadata_other_test.go`.  
**Start:** WU4D1 is a compiling, passing resolver and accepted-facts safety boundary; exhaustive concrete error typing, hostile numeric/non-Stat facts, close-failure precedence, and Linux typed evidence remain absent.  
**Finish:** all eight metadata failure classes—missing, symlink, non-directory, device mismatch, non-local, inaccessible, changed-between-observations, and invalid metadata—have concrete `MetadataPathError` `errors.Is`/`errors.As` behavior; zero/negative/overflow/non-`Stat_t` facts fail closed; close failure has exact primary-error precedence; every adversarial path has exact close counts; and non-Darwin unsupported construction preserves `errors.Is`/`errors.As`.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestDarwinMetadataErrorClasses|TestDarwinMetadataInvalidFacts|TestDarwinMetadataCloseFailurePrecedence|TestDarwinMetadataAdversarialLifecycle|TestUnsupportedEnvironmentErrors)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized disposable lifecycle/error fixtures are the bounded harness; no scanner or CLI exists.  
**Rollback boundary:** restore the exact accepted WU4D1 hashes and remove `metadata_other_test.go` if WU4D2 created it; the compiling WU4D1 resolver remains available and WU5 stays blocked. Keep `/tmp/osdy-wu4d-baseline-1788066883-49900/` until WU4D2 verification and receipt completion.

- [x] **WU4D2 RED:** In `internal/platform/macos/metadata_darwin_test.go` and `metadata_other_test.go`, add failing concrete `errors.Is`/`errors.As` cases for all eight metadata classes; zero device/inode/link count, negative size/blocks, allocation overflow, and non-`syscall.Stat_t`; close failure as primary only after otherwise successful acquisition while an earlier open/fstat/fstatfs/revalidation failure remains primary; exact close counts for every adversarial root/intermediate/final path; and Linux unsupported `errors.Is`/`errors.As`; run the WU4D2 focused command and capture a non-zero RED caused only by missing WU4D2 behavior. <!-- sdd-owner: implementation -->
- [x] **WU4D2 GREEN:** Implement the minimum remaining concrete `MetadataPathError` classification, fail-closed numeric/non-Stat validation, deferred-close error capture with primary-error precedence, and typed non-Darwin unsupported behavior while preserving WU4D1 resolver flags, accepted-fact rules, and all close paths; rerun the WU4D2 focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4D2 TRIANGULATE:** Add combined open/fstat/fstatfs/revalidation/close failures, all root/intermediate/final close-count permutations, repeated close-error and invalid-field cases, and Linux construction assertions; prove no close error masks an earlier primary failure, successful acquisition plus close failure returns inaccessible with no accepted `Metadata`, unsupported errors retain both `errors.Is` and `errors.As`, and no descriptor or target fact escapes; rerun the focused command plus `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test` and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4D2 REFACTOR:** Run `gofmt -w internal/platform/macos/*.go && go test ./internal/platform/macos -run '^(TestDarwinMetadataErrorClasses|TestDarwinMetadataInvalidFacts|TestDarwinMetadataCloseFailurePrecedence|TestDarwinMetadataAdversarialLifecycle|TestUnsupportedEnvironmentErrors)$' -count=1 && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos)"`; expect focused/platform/Linux/full/vet/format PASS, record exact commands/results, runtime N/A, accepted-WU4D1 predecessor hashes, final hashes, rollback boundary, and WU4D2 additions plus deletions at no more than 400; retain the baseline reproduction directory through this independent verification, then authorize WU5. <!-- sdd-owner: implementation -->

### WU4E — Correct Reachable Metadata Errors and Changed-Entry Ownership

**Forecast:** 110–180 additions, 40–100 deletions, 150–280 authored changed lines; independently capped at 400 and accounted from the checked WU4D2 candidate.  
**Depends on:** checked WU4D2 implementation plus failed independent D2 verification identifying artificial adapter-owned changed-entry surfaces.  
**Allowed edit surfaces:** `internal/platform/macos/metadata_darwin.go`, `internal/platform/macos/metadata_other.go`, `internal/platform/macos/metadata_darwin_test.go`, `internal/platform/macos/metadata_other_test.go`.  
**Start:** the WU4D2 candidate passes its recorded suite, but production/test seams still expose `statType`, `revalidate`, non-`Stat_t` simulation, and adapter-level `ErrMetadataChanged`; independent D2 verification is failed.  
**Finish:** the adapter exposes only production-reachable descriptor operations and error classes; missing, symlink, non-directory, device mismatch, non-local filesystem, inaccessible, invalid numeric metadata, descriptor closure/close precedence, and non-Darwin unsupported behavior remain tested, while scanner-owned changed-entry evidence is deferred to WU5.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestDarwinMetadataProductionSeams|TestDarwinMetadataErrorClasses|TestDarwinMetadataInvalidFacts|TestDarwinMetadataCloseFailurePrecedence|TestDarwinMetadataAdversarialLifecycle|TestDarwinMetadataDescriptorStability|TestUnsupportedEnvironmentErrors)$' -count=1`.  
**Runtime harness evidence:** N/A — disposable descriptor fixtures are the bounded metadata harness; scanner enumeration-versus-inspection behavior begins only in WU5.  
**Rollback boundary:** restore the checked WU4D2 hashes: `metadata_darwin.go` `47f6b087cda8eb5f89e8a8d386573d46aea941f978b166f46778f2b6b86b3dfc`, `metadata_other.go` `81e4a9cb7469076a4cfb17eddea6dead533938bc08428954c600ab3a5f591ad0`, `metadata_darwin_test.go` `77237653df8f6e02b515baab07a94b277e85316d3e01349ff69c6e720d81aea8`, and `metadata_other_test.go` `85c4721901e7c95cb56aa35a14b31e16197cedfa0b1478f9263ac8de66892fab`; this restores only the failed D2 candidate and keeps WU5 absent.

- [x] **WU4E RED:** In `internal/platform/macos/metadata_darwin_test.go` and `metadata_other_test.go`, add tests first proving production `MetadataAdapter`/`componentOps` expose no `statType` or `revalidate` hook and neither Darwin nor non-Darwin exposes an adapter-level changed sentinel; retain tests for every reachable error class, invalid numeric facts, descriptor close counts/primary-error precedence, and typed unsupported environment. Confirm the adjacent WU5 task contract—not scanner tests in this unit—covers changed kind/identity between enumeration evidence and later descriptor metadata; run the WU4E focused command and capture RED against the artificial surfaces. <!-- sdd-owner: implementation -->
- [x] **WU4E GREEN:** In the four allowed metadata files, remove `statType`, `revalidate`, `ErrMetadataChanged`, their production branches, and tests that fabricate non-`syscall.Stat_t` or adapter-owned change results; preserve WU4D1 component-wise no-follow resolution, final-descriptor `Fstat`/`Fstatfs` facts, regular-file-only bytes, every reachable metadata error, invalid numeric rejection, descriptor lifecycle/close precedence, and typed non-Darwin unsupported behavior; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU4E TRIANGULATE:** Add a synchronized descriptor-stability case proving pathname replacement after final-descriptor acquisition cannot change the accepted descriptor-backed mode/device/inode/link/size/blocks/locality facts; state explicitly that comparison of enumeration evidence with later inspection is deferred to WU5, rerun every test in `internal/platform/macos`, and expect PASS with no changed sentinel, revalidation hook, or impossible runtime fact type. <!-- sdd-owner: implementation -->
- [x] **WU4E REFACTOR:** Run `gofmt -w internal/platform/macos/*.go && go test ./internal/platform/macos -run '^(TestDarwinMetadataProductionSeams|TestDarwinMetadataErrorClasses|TestDarwinMetadataInvalidFacts|TestDarwinMetadataCloseFailurePrecedence|TestDarwinMetadataAdversarialLifecycle|TestDarwinMetadataDescriptorStability|TestUnsupportedEnvironmentErrors)$' -count=1 && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos)"`; expect focused/platform/Linux/full/vet/format PASS, independently measure additions plus deletions at no more than 400, and record exact predecessor/final file hashes, command results, runtime N/A, and the four-file rollback boundary before authorizing WU5. <!-- sdd-owner: implementation -->

### WU5C1A — Accept the Platform-Neutral Descriptor Contract Only

**Forecast:** 130–190 additions, 10–30 deletions, 140–220 authored changed lines; independently capped at 400.  
**Depends on:** accepted WU4E, rejected combined WU5C1 evidence `sha256:614522...`, and exact rollback of `internal/platform/macos/walker_darwin.go`, `walker_darwin_test.go`, and `walker_other.go` while retaining `internal/scan/descriptor.go` and `descriptor_test.go` only as an unaccepted candidate.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go`. No Darwin build tags, syscalls, raw descriptors, or platform adapter files.  
**Start:** first freshly retain a reproducible baseline because the old `/tmp` baseline is absent; after acquisition rollback, only the 146-line combined-candidate contract/test surface remains unaccepted.  
**Finish:** immutable clean absolute/relative component values encode root policy and relative chains; limit semantics, platform-neutral `RootFacts`/identity, opaque `TrustedRoot`/`DescriptorWalker` ownership, stable error classes, and concrete path/operation context with `errors.Is`/`errors.As` are fully specified. Fake ports prove capability ownership and lifecycle without Darwin behavior.  
**Focused command:** `go test ./internal/scan -run '^(TestDescriptorComponents|TestDescriptorRootPolicy|TestDescriptorRelativeChains|TestDescriptorLimits|TestDescriptorFactsAndIdentity|TestDescriptorOwnership|TestDescriptorPathError)$' -count=1`.  
**Runtime harness evidence:** N/A — this is a platform-neutral value/ownership contract with fake ports and no syscall or executable boundary.  
**Rollback boundary:** remove `internal/scan/descriptor.go` and `descriptor_test.go`, returning product code to accepted WU4E while leaving the fresh evidence baseline outside the repository.

- [x] **WU5C1A RED:** In `internal/scan/descriptor_test.go`, replace broad regex-only evidence with exact named tests for immutable clean absolute and relative component values, root policy, relative chains, limit validation/meaning, platform-neutral `RootFacts` and identity, opaque `TrustedRoot`/`DescriptorWalker` ownership, stable classes, and concrete `PathError` operation/component context supporting `errors.Is` and `errors.As`; run the WU5C1A focused command and capture failures only for missing or incorrect contract behavior. <!-- sdd-owner: implementation -->
- [x] **WU5C1A GREEN:** In `internal/scan/descriptor.go`, implement the minimum syscall-free immutable values, root policy, relative chains, limits, facts/identity, opaque capability interfaces, stable sentinels, and typed `PathError`; deep-copy every component input/output and expose neither raw handles nor platform constants; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C1A TRIANGULATE:** In `internal/scan/descriptor_test.go`, add fake-port ownership cases for retained value immutability, rejected empty/`.`/`..`/slash/NUL components, root-versus-relative policy, every limit edge, capability transfer/close ownership, wrapped primary classes, and precise component-specific context; prove fake visitors cannot retain or close a platform handle and rerun the focused command. <!-- sdd-owner: implementation -->
- [x] **WU5C1A REFACTOR:** Run `gofmt -w internal/scan/descriptor.go internal/scan/descriptor_test.go && go test ./internal/scan -run '^(TestDescriptorComponents|TestDescriptorRootPolicy|TestDescriptorRelativeChains|TestDescriptorLimits|TestDescriptorFactsAndIdentity|TestDescriptorOwnership|TestDescriptorPathError)$' -count=1 && go test ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; expect PASS, verify the two files contain no Darwin/syscall/raw-handle surface, and record the fresh predecessor/final hashes, <=400 accounting, runtime N/A, and two-file rollback before WU5C1B. <!-- sdd-owner: implementation -->

### WU5C1A2 — Correct the Fail-Closed Contract, Limit Semantics, and Root Ownership

**Forecast:** 90–150 additions, 10–40 deletions, 100–190 authored changed lines; independently capped at 400 and not cumulative with WU5C1A's exact +134/-266=400.  
**Depends on:** checked WU5C1A candidate hashes `descriptor.go` `38823c837ab22c3373ab3c148bccf938b13c5361bf290233d77f43dbda362d47` and `descriptor_test.go` `ef645382fa9f6d6a42c5456e95fa16894ee92ab32888ea513c190c77b7e8522b`, plus independent C1A verification FAIL despite passing tests/accounting.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go` only. No platform code, design, specification, proposal, or documentation artifacts.  
**Start:** retain a fresh reproducible two-file baseline matching the exact checked C1A hashes; `NewPathError` currently fabricates missing context/classification, `WalkLimits` lacks complete edge semantics and API comments, and the fake root test neither transfers through a meaningful `DescriptorWalker` nor proves idempotent typed close caching.  
**Finish:** `NewPathError(operation, path, class)` returns `(*PathError, error)` and rejects empty operation, empty path, or nil class without panic or fabricated facts; exported limit fields document and validate DFS component depth versus simultaneous active capability budget; a fake walker transfers one opaque root whose repeated `Close` calls invoke the underlying close once and return the cached typed result.  
**Focused command:** `go test ./internal/scan -run '^(TestDescriptorPathErrorRejectsInvalidContext|TestDescriptorLimitSemantics|TestDescriptorOwnershipTransfer|TestDescriptorCloseIdempotence)$' -count=1`.  
**Runtime harness evidence:** N/A — this is a platform-neutral two-file contract correction with no syscall, goroutine, scanner, or executable boundary.  
**Rollback boundary:** restore exactly the two checked WU5C1A file hashes above from the fresh WU5C1A2 baseline; no walker/platform file or later work unit is touched.

- [x] **WU5C1A2 RED:** In `internal/scan/descriptor_test.go`, add exact named tests requiring `NewPathError(operation, path, class) (*PathError, error)` to reject empty operation, empty path, and nil class without panic or fabricated operation/path/error; require `WalkLimits` API comments and exact validation cases for depth=0, descriptors=0, `{1,1}`, depth-heavy/descriptor-light, and depth-light/descriptor-heavy values with semantics observable as DFS component depth and simultaneous active capability budget; and require a meaningful fake `DescriptorWalker` to transfer one opaque `TrustedRoot` whose repeated `Close` calls invoke the underlying close once and return the same cached typed result; run the focused command and capture genuine failures against the checked C1A candidate. <!-- sdd-owner: implementation -->
- [x] **WU5C1A2 GREEN:** In `internal/scan/descriptor.go` and `descriptor_test.go`, implement only the minimum panic-free fail-closed `NewPathError` error-returning API, exported `WalkLimits` field comments and independent positive-limit validation, and opaque trusted-root ownership helper/fixture needed for one-time underlying close with cached typed result; update existing callers/tests for the constructor result and add no platform code or raw capability surface; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C1A2 TRIANGULATE:** Extend the focused tests with wrapped `PathError` classes/context, invalid-constructor permutations, asymmetric `{1,64}` and `{64,1}` limits, successful and failing underlying close results, repeated-close identity/`errors.Is`/`errors.As` checks, and fake-walker handoff/consumer permutations; prove no context or class is invented, limit dimensions remain independent, the capability is transferred only once, and every close returns the cached typed result. <!-- sdd-owner: implementation -->
- [x] **WU5C1A2 REFACTOR:** Run `gofmt -w internal/scan/descriptor.go internal/scan/descriptor_test.go && go test ./internal/scan -run '^(TestDescriptorPathErrorRejectsInvalidContext|TestDescriptorLimitSemantics|TestDescriptorOwnershipTransfer|TestDescriptorCloseIdempotence)$' -count=1 && go test ./internal/scan -count=1 && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/scan/descriptor.go internal/scan/descriptor_test.go)"`; expect focused/package/full/vet/format PASS, verify no platform/syscall/raw-handle code, independently measure at most 400 authored additions plus deletions from the two-file C1A baseline, and record exact predecessor/final hashes, runtime N/A, and two-file rollback before WU5C1B. <!-- sdd-owner: implementation -->

### WU5C1B1 — Acquire the Darwin Core Trusted Root

**Forecast:** 250–320 additions, 0–30 deletions, 250–350 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5C1A2 and the completed +434/-0 WU5C1B candidate rollback.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_other.go`, `internal/platform/macos/walker_darwin_test.go`. No directory enumeration or scanner/finalizer edits.  
**Start:** accepted scan-owned contract exists; no walker files remain after rollback.  
**Finish:** constructible Darwin/non-Darwin walkers provide minimal trusted `/` (read-only) → home → root acquisition, exact safe flags, final-descriptor facts, every post-home component gate, core typed errors, typed unsupported behavior, and opaque `NewTrustedRoot` transfer without enumeration. Exhaustive cancellation, limit, and close matrices are deferred to WU5C1B2.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestNewDescriptorWalker|TestWalkerRootHomeAncestryPreFSValidation|TestWalkerAcquisitionOrderAndFlags|TestWalkerComponentFacts|TestWalkerEveryPostHomeComponentGate|TestWalkerCoreErrorClassification|TestWalkerUnsupportedBoundary|TestWalkerIntermediateDevicePoison)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized descriptor-operation seams are the bounded core acquisition harness; no scanner or CLI exists.  
**Rollback boundary:** remove the three WU5C1B1 walker files, preserving independently accepted WU5C1A2 contract files.

- [x] **WU5C1B1 RED:** In `internal/platform/macos/walker_darwin_test.go`, add the exact named tests `TestNewDescriptorWalker`, `TestWalkerRootHomeAncestryPreFSValidation`, `TestWalkerAcquisitionOrderAndFlags`, `TestWalkerComponentFacts`, `TestWalkerEveryPostHomeComponentGate`, `TestWalkerCoreErrorClassification`, `TestWalkerUnsupportedBoundary`, and `TestWalkerIntermediateDevicePoison`; require a constructible `NewDescriptorWalker`, root/home ancestry and pre-filesystem validation, exact `/` (read-only) plus component relative-open order and `O_RDONLY|O_NOFOLLOW|O_DIRECTORY|O_CLOEXEC|O_NONBLOCK` flags, component-specific final-descriptor facts, device/local/type/nonzero gates after home before every deeper open, core typed missing/symlink/non-directory/inaccessible/device/non-local/invalid classification, a typed unsupported compile/semantic boundary, and device-9 intermediate poison whose child must not open; run the B1 focused command and capture genuine missing-acquisition RED. <!-- sdd-owner: implementation -->
- [x] **WU5C1B1 GREEN:** Implement only minimal acquisition in `internal/platform/macos/walker_darwin.go` and `walker_other.go`: construct the walker, open trusted `/` (read-only), home ancestry, and root suffix descriptor-relatively with exact safe flags, accept only final-descriptor facts, gate every post-home component before opening deeper, transfer the accepted capability through `scan.NewTrustedRoot`, return typed component-specific/core or unsupported errors, and add no enumeration, exhaustive cancellation/limit matrix, or exhaustive close lifecycle. <!-- sdd-owner: implementation -->
- [x] **WU5C1B1 TRIANGULATE:** In `walker_darwin_test.go`, cover every component position and root/home permutation; prove only final-descriptor facts are accepted; classify missing, symlink, non-directory, wrong-device, non-local, and invalid/nonzero facts; and repeat the intermediate-device poison gate so no deeper child opens and no target fact escapes. <!-- sdd-owner: implementation -->
- [x] **WU5C1B1 REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin.go internal/platform/macos/walker_other.go internal/platform/macos/walker_darwin_test.go && go test ./internal/platform/macos -run '^(TestNewDescriptorWalker|TestWalkerRootHomeAncestryPreFSValidation|TestWalkerAcquisitionOrderAndFlags|TestWalkerComponentFacts|TestWalkerEveryPostHomeComponentGate|TestWalkerCoreErrorClassification|TestWalkerUnsupportedBoundary|TestWalkerIntermediateDevicePoison)$' -count=1 && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos/walker_darwin.go internal/platform/macos/walker_other.go internal/platform/macos/walker_darwin_test.go)"`; expect focused/platform/Linux compile/full/vet/format PASS, record exact absent-file baseline, hashes, runtime N/A, rollback, and additions plus deletions at no more than 400, and explicitly defer exhaustive cancellation/limits/close matrices to WU5C1B2. <!-- sdd-owner: implementation -->

### WU5C1B1A — Fail Closed on Acquisition Handoff Close Failure

**Forecast:** 70–120 additions, 5–25 deletions, 75–145 authored changed lines; independently capped at 400.  
**Depends on:** the checked WU5C1B1 candidate and independent verifier FAIL identifying a reachable descriptor handoff leak.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_other.go`, `internal/platform/macos/walker_darwin_test.go`. No API expansion, enumeration, scanner, or finalizer edits.  
**Start:** the exact three-file B1 baseline is 381 authored additions with `walker_darwin.go` `ba2910bfee5639b492e1ef718c6c05694583ea304e1ae78f779802cb0d4316a4`, `walker_other.go` `32a7f4dc2f588ac226b178b3cec700cb4309ca770eeec6c1fac418bbd05c8b72`, and `walker_darwin_test.go` `7bfb70431fe1dbe1158d50a3ec15c74a89c13a63e48f57c7af76d4fad501b562`; `AcquireRoot` currently ignores an intermediate `close(current)` failure after the next descriptor is opened and fact-validated.  
**Finish:** acquisition fails closed at the reachable handoff: it preserves typed primary-error precedence, attempts every possible close, closes the newly validated descriptor exactly once, performs no deeper open, and never returns `TrustedRoot`; successful handoff behavior is unchanged.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionHandoffCloseFailure|TestWalkerAcquisitionHandoffPrimaryPrecedence)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized descriptor-operation seams are the bounded handoff ownership harness; no scanner or CLI exists.  
**Rollback boundary:** restore the exact three B1 hashes above, removing only B1A handoff-close tests and fail-closed correction.

- [x] **WU5C1B1A RED:** In `internal/platform/macos/walker_darwin_test.go`, add exact `TestWalkerAcquisitionHandoffCloseFailure` and `TestWalkerAcquisitionHandoffPrimaryPrecedence` cases where the next component is opened and fact-validated before closing the previous descriptor fails; require acquisition to return a typed inaccessible close `PathError`, close the new descriptor exactly once, perform no deeper open, and return no `TrustedRoot`; combine an earlier primary failure with cleanup close failures to prove the primary is preserved while every possible close is attempted, focusing only on this reachable handoff leak rather than B2's exhaustive matrix. <!-- sdd-owner: implementation -->
- [x] **WU5C1B1A GREEN:** In `internal/platform/macos/walker_darwin.go`, implement the minimum fail-closed handoff correction so a failed `close(current)` after successful next-descriptor validation stops acquisition, closes the new descriptor exactly once, returns typed component-specific inaccessible/close context, and never transfers `TrustedRoot`; preserve all B1 APIs, order, flags, facts, gates, unsupported behavior, and no-enumeration boundary. <!-- sdd-owner: implementation -->
- [x] **WU5C1B1A TRIANGULATE:** Extend the focused synchronized seams across root→home and post-home handoff positions, repeated close errors, and earlier-primary-plus-cleanup combinations; prove component context is stable, every possible descriptor close is attempted once, no deeper component opens after handoff failure, no capability escapes, and successful handoff acquisition remains unchanged. <!-- sdd-owner: implementation -->
- [x] **WU5C1B1A REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin.go internal/platform/macos/walker_other.go internal/platform/macos/walker_darwin_test.go && go test ./internal/platform/macos -run '^(TestWalkerAcquisitionHandoffCloseFailure|TestWalkerAcquisitionHandoffPrimaryPrecedence)$' -count=1 && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos/walker_darwin.go internal/platform/macos/walker_other.go internal/platform/macos/walker_darwin_test.go)"`; expect focused/platform/Linux compile/full/vet/format PASS, independently measure additions plus deletions at no more than 400 against the exact three-file B1 baseline, and record baseline/final hashes, runtime N/A, rollback, and preservation of successful handoff before authorizing B2. <!-- sdd-owner: implementation -->

### WU5C1B2 — Harden Acquisition Cancellation, Limits, and Full Lifecycle

**Forecast:** 140–210 additions, 0–30 deletions, 140–240 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5C1B1A with exact hashes and rollback evidence.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_other.go`, `internal/platform/macos/walker_darwin_test.go`. No enumeration or scanner/finalizer edits.  
**Start:** B1 core acquisition and essential gates pass, while exhaustive cancellation, limits, and ownership lifecycle matrices remain intentionally absent.  
**Historical claim (rejected):** every cancellation checkpoint and acquisition limit was claimed enforced and lifecycle coverage complete, but independent verification found missing semantics and coverage; these checked rows are not acceptance evidence.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionCancellationCheckpoints|TestWalkerAcquisitionLimits|TestWalkerAcquisitionHandleClose|TestWalkerAcquisitionLifecycle|TestWalkerAcquisitionCloseCounts|TestWalkerAcquisitionErrorPrecedence)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized lifecycle seams are the bounded acquisition harness; no scanner or CLI exists.  
**Rollback boundary:** restore exact accepted WU5C1B1A hashes for the three allowed files, removing only B2 lifecycle hardening and tests.  
**Independent status:** FAIL — checked rows are preserved historical implementation evidence only; B2 is not accepted and satisfies no dependency.

- [x] **WU5C1B2 RED:** In `internal/platform/macos/walker_darwin_test.go`, add the exact named tests `TestWalkerAcquisitionCancellationCheckpoints`, `TestWalkerAcquisitionLimits`, `TestWalkerAcquisitionHandleClose`, `TestWalkerAcquisitionLifecycle`, `TestWalkerAcquisitionCloseCounts`, and `TestWalkerAcquisitionErrorPrecedence`; require cancellation before/open/fstat/fstatfs/validation/handoff, every depth/descriptor limit edge, exact handle-close counts for success and every open/fstat/fstatfs/validation/handoff failure, primary-plus-cleanup precedence, and idempotent typed handle close with one underlying close and the cached result. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2 GREEN:** Make only the minimum `walker_darwin.go`/`walker_other.go` corrections needed for cancellation checkpoints, limit enforcement, complete acquisition ownership cleanup, typed primary-error preservation with secondary close evidence, and idempotent cached typed handle close; preserve B1 order, flags, gates, facts, unsupported behavior, and no-enumeration boundary. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2 TRIANGULATE:** Extend the synchronized matrix across every component position and cancellation/limit boundary, all success/open/fstat/fstatfs/validation/handoff/close combinations, repeated successful and failing close calls, and combined primary-plus-cleanup errors; prove exact opens/closes, no deeper work after stop, no capability leak, and stable `errors.Is`/`errors.As` context and precedence. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2 REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin.go internal/platform/macos/walker_other.go internal/platform/macos/walker_darwin_test.go && go test ./internal/platform/macos -run '^(TestWalkerAcquisitionCancellationCheckpoints|TestWalkerAcquisitionLimits|TestWalkerAcquisitionHandleClose|TestWalkerAcquisitionLifecycle|TestWalkerAcquisitionCloseCounts|TestWalkerAcquisitionErrorPrecedence)$' -count=1 && go test ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos/walker_darwin.go internal/platform/macos/walker_other.go internal/platform/macos/walker_darwin_test.go)"`; expect focused/platform/Linux compile/full/vet/format PASS and persist accepted-B1 baseline/final hashes, exact <=400 accounting, runtime N/A, and rollback before WU5C1C. <!-- sdd-owner: implementation -->

### WU5C1B2A — Correct Reachable Cancellation and Acquisition Capability Limits

**Forecast:** 140–220 additions, 10–40 deletions, 150–260 authored lines; ≤400, one writer.  
**Depends on:** checked B2 as failed historical baseline only; parent first creates a byte-exact immutable baseline containing the current product files, `tasks.md`, `apply-progress.md`, and self-excluding manifest.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`; `internal/scan/descriptor.go` and `internal/scan/descriptor_test.go` are read-only evidence because their accepted docs/tests already define depth below the acquired root, simultaneous active capabilities, zero invalidity, and positive asymmetric validity; no enumeration/scanner/finalizer.  
**Finish:** `AcquireRoot` validates both `WalkLimits` fields as positive, never consumes `MaxDepth` for `/` (read-only) → home → root acquisition, and enforces `MaxDescriptors` as simultaneous active opaque capabilities: `1` fails before a second capability, `2` succeeds at the exact boundary, and `64` succeeds; independently valid `MaxDepth` values `1` and `64` do not alter acquisition. Reachable synchronized checkpoints stop at before/open/fstat/fstatfs/validation/handoff without artificial domain errors, close every opened descriptor once, assert exact no-deeper operation/open, return no capability after a stop, and preserve B1A/no-enumeration. Exact DFS-depth success and one-below failure are deferred to WU5C2 descriptor enumeration/descent.  
**Focused command:** `go test ./internal/scan -run '^TestDescriptorLimitSemantics$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerAcquisitionCancellationReachability|TestWalkerAcquisitionLimitSemantics)$' -count=1`.  
**Rollback:** restore the parent-created failed-B2 immutable baseline; historical B2B could not begin until independent semantic verification accepted B2A.

- [x] **WU5C1B2A RED:** Add exact `TestWalkerAcquisitionCancellationReachability` with a synchronized operation seam that cancels distinctly after open, fstat, fstatfs, validation, and before handoff—never remapping checkpoints or injecting artificial domain errors—and exact `TestWalkerAcquisitionLimitSemantics` rows proving `MaxDescriptors` `1` fails before a second simultaneous capability, `2` succeeds at the exact boundary, and `64` succeeds while positive `MaxDepth` values `1` and `64` leave acquisition unchanged. Run existing read-only `TestDescriptorLimitSemantics` evidence for zero-value invalidity, positive asymmetric `{1,64}`/`{64,1}` validity, depth-below-root docs, and simultaneous-capability docs; capture genuine semantic RED with exact no-deeper-work/open and close-count assertions. Do not add acquisition exact-depth success or one-below failure; WU5C2 owns those DFS cases. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2A GREEN:** Implement only reachable deterministic cancellation checkpoints (a synchronized operation seam is allowed), positive limit validation, and exact `MaxDescriptors` simultaneous-capability enforcement in the two writable platform files; do not consume `MaxDepth` from the `/` (read-only) → home → root component count. Close owned descriptors once, return no `TrustedRoot` after cancellation or descriptor-limit stop, and preserve B1A order, flags, gates, typed errors, and no-enumeration. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2A TRIANGULATE:** Exercise every acquisition component position, `MaxDescriptors` `1`/`2`/`64`, and invariant positive `MaxDepth` `1`/`64`; assert the named checkpoint was reached, the immediately deeper operation was not, exact opens/closes and typed context, no capability leak, exact-boundary acquisition success at two simultaneous capabilities, and no acquisition behavior change from DFS-depth values. Leave exact descendant-depth boundary exercise to WU5C2. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2A REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin.go internal/platform/macos/walker_darwin_test.go && go test ./internal/scan -run '^TestDescriptorLimitSemantics$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerAcquisitionCancellationReachability|TestWalkerAcquisitionLimitSemantics)$' -count=1 && go test ./internal/scan ./internal/platform/macos -count=1 && GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test && go test ./... -count=1 && go vet ./...`; verify `internal/scan/descriptor.go` and `descriptor_test.go` remain byte-identical, no enumeration exists, `AcquireRoot` does not consume acquisition path length as depth, record exact hashes/+−/rollback against the immutable failed-B2 baseline, and stop for independent verification. <!-- sdd-owner: implementation -->

### WU5C1B2B — Acquisition Lifecycle Matrix Evidence (Historical Verification-Only; Independent FAIL)

**Actual accounting:** `walker_darwin.go` +0/-0, `walker_other.go` +0/-0, `walker_darwin_test.go` +266/-1; 267 authored changed lines, ≤400.  
**Depends on:** independently accepted B2A receipt and the verified B2A baseline.  
**Allowed edit surfaces used:** `internal/platform/macos/walker_darwin_test.go` only; no production, enumeration, scanner, or finalizer edit occurred.  
**Historical result:** the initial failures were wrong test-oracle failures corrected without production change. Preserve all checked rows as historical test-only verification/triangulation evidence, never genuine behavioral RED→GREEN or acceptance. Independent verifier `subtask_gentle-ai-verify_1788112997031_636b270f` found 12 omitted combinations: fstat/fstatfs/validation primary failures at component positions 1–4 where both next and current are owned but only next-close failure was injected. Cancellation multi-close cases do not cover them.  
**Focused command used:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionLifecycleComponentMatrix|TestWalkerAcquisitionClosePrecedenceMatrix)$' -count=1`.  
**Independent status:** FAIL — B2B satisfies no dependency; C is blocked on independently accepted B2B1.

- [x] **WU5C1B2B RED (historical label only):** Added `TestWalkerAcquisitionLifecycleComponentMatrix` and `TestWalkerAcquisitionClosePrecedenceMatrix`; initial failures were incorrect attempted-open/validation-order oracles, not a production behavior failure, so this is verification evidence and not genuine RED. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2B GREEN (reclassified):** Corrected only the test oracles and observed PASS on unchanged production; this is verification-only PASS, never behavioral GREEN. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2B TRIANGULATE (incomplete):** Added lifecycle and close cases, multiple secondaries, and repeated handle-close evidence, but omitted the 12 primary × owned-current-close combinations identified above. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2B REFACTOR (historical):** Recorded focused/platform/Linux/full/vet/format PASS and +266/-1 test-only accounting; independent FAIL supersedes any acceptance implication. <!-- sdd-owner: implementation -->

### WU5C1B2B1 — Missing Primary × Owned-Current-Close Cartesian Evidence (Historical Verification-Only; Independent FAIL)

**Forecast:** 60–140 additions, 0 deletions, 60–140 authored lines; ≤400, one writer.  
**Depends on:** independently accepted B2A plus failed B2B as historical test-only evidence; B2B itself satisfies no dependency.  
**Allowed edit surface:** `internal/platform/macos/walker_darwin_test.go` only. No production, scan, design, spec, docs, enumeration, scanner, or finalizer edits.  
**Start:** current production passes existing tests, but the matrix omits exactly 12 combinations: each fstat/fstatfs/validation primary at component positions 1–4 with both next/current owned and a close failure injected specifically on current.  
**Finish:** exact `TestWalkerAcquisitionPrimaryCleanupCartesian` (or an explicit generated extension of `TestWalkerAcquisitionClosePrecedenceMatrix`) covers all 12 combinations with case-specific assertions: primary error remains first and preserves `errors.Is`/`errors.As`; typed current-close secondary is discoverable; next/current are each attempted exactly once; no deeper operation/open occurs; no `TrustedRoot`, capability, or fact escapes. It also verifies multiple secondary errors and existing handle-close caching without duplicating B2B.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionPrimaryCleanupCartesian|TestWalkerAcquisitionClosePrecedenceMatrix)$' -count=1`.  
**Rollback:** remove only B2B1 test rows/helpers from `internal/platform/macos/walker_darwin_test.go`, restoring the independently failed B2B three-file hashes. Verifier `subtask_gentle-ai-verify_1788113912097_c4b25af5` independently rejected this checked attempt because secondary close class was unasserted; B2B1 satisfies no dependency and C is blocked on independently accepted B2B1A.

- [x] **WU5C1B2B1 VERIFY:** Add the exact 12 generated cases in `internal/platform/macos/walker_darwin_test.go` and run the focused command. If all correct expectations pass on current production, record verification-only PASS with no RED obligation; if any correct expectation fails, stop and plan a new genuine RED→GREEN behavior-correction unit before any production edit. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2B1 TRIANGULATE:** Add case-specific assertions for primary-first `errors.Is`/`errors.As`, discoverable typed current-close secondary, one attempted close each for next/current, exact stop boundary, nil capability/no fact escape, plus non-duplicative multiple-secondary and cached repeated-handle-close checks. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2B1 REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin_test.go && go test ./internal/platform/macos -run '^(TestWalkerAcquisitionPrimaryCleanupCartesian|TestWalkerAcquisitionClosePrecedenceMatrix)$' -count=1`; expect PASS, record exact file hashes and +/− accounting against failed B2B, verify ≤400 and test-only scope, then stop for independent verification before C. <!-- sdd-owner: implementation -->

### WU5C1B2B1A — Correct Secondary-Close Class Verification

**Forecast:** 10–40 additions, 0 deletions, 10–40 authored changed lines; ≤400, one writer.  
**Depends on:** independently accepted B2A plus checked B2B1 as an independently failed/not-accepted historical verification attempt; B2B1 satisfies no dependency.  
**Allowed edit surface:** `internal/platform/macos/walker_darwin_test.go` only. No production, scan, design, spec, docs, enumeration, scanner, finalizer, commit, or review work.  
**Start:** verifier `subtask_gentle-ai-verify_1788113912097_c4b25af5` rejected B2B1 because `assertSecondaryCloseEvidence` checks a typed `scan.PathError` close operation/path but does not assert its class. All other B2B1 semantics and checks passed.  
**Finish:** `assertSecondaryCloseEvidence` requires `errors.Is(typedClose, scan.ErrInaccessible)` for each expected typed close, thereby covering all 12 existing fstat/fstatfs/validation × component-position current-close cases without changing production. If the correct class assertion fails, stop for a separately planned genuine behavior correction before any production edit.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionPrimaryCleanupCartesian|TestWalkerAcquisitionClosePrecedenceMatrix)$' -count=1`.  
**Rollback:** remove only the B2B1A class assertion and its adjacent diagnostic from `internal/platform/macos/walker_darwin_test.go`, restoring checked failed-B2B1 test hash `5a4559bb248b0a3cf463a8fa5272bab8f18affc4a052a45111542dfc7f6ad831`. Production hashes must remain `walker_darwin.go` `beca553e58802696ba9af5d21281ef8707623d25d5ee92af302886e099ab644b` and `walker_other.go` `32a7f4dc2f588ac226b178b3cec700cb4309ca770eeec6c1fac418bbd05c8b72`.

- [x] **WU5C1B2B1A VERIFY:** In `internal/platform/macos/walker_darwin_test.go`, minimally update `assertSecondaryCloseEvidence` so every matched typed close also satisfies `errors.Is(typedClose, scan.ErrInaccessible)`, then run the exact focused B2B1/precedence command. If it passes, record verification-only PASS with no RED obligation; if it fails, stop for a genuine behavior-correction plan and do not edit production. <!-- sdd-owner: implementation -->
- [x] **WU5C1B2B1A REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin_test.go`, the exact focused command, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test`, `go test ./... -count=1`, `go vet ./...`, and `test -z "$(gofmt -d internal/platform/macos/walker_darwin_test.go)"`; record exact additions/deletions, final test hash, production `cmp`/hash byte-identity, runtime N/A, rollback, and stop for independent verification before C. <!-- sdd-owner: implementation -->

### WU5C1C1 — Verify Real Darwin Descriptor Binding and Mount Poison Rejection

**Forecast:** 160–280 additions, 0 deletions, 160–280 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5C1B2B1A; the failed historical WU5C1C +123-line candidate and failed evidence `sha256:a126a6cb18d2d79f905714e7bc613ec8378efd11dc662245c18cbf114848cd99` satisfy no dependency.  
**Allowed edit surface:** `internal/platform/macos/walker_darwin_test.go` only. Production walker files are read-only; no enumeration, scanner, finalizer, design, docs, or product edits.  
**Start:** the exact restored baseline is `walker_darwin.go` `beca553e58802696ba9af5d21281ef8707623d25d5ee92af302886e099ab644b`, `walker_other.go` `32a7f4dc2f588ac226b178b3cec700cb4309ca770eeec6c1fac418bbd05c8b72`, and `walker_darwin_test.go` `9d751137039eabce28e444393d40c3378687029174f8c7ad1604d622bf6afdb7`.  
**Finish:** exact named tests `TestWalkerAcquisitionRealAncestorReplacement`, `TestWalkerAcquisitionRealFinalReplacement`, `TestWalkerAcquisitionRealNoFollowBinding`, and `TestWalkerAcquisitionMountPoisonScript` prove acquisition against an actual `t.TempDir()` component tree. A synchronization-only wrapper delegates facts to real Darwin `openat`/`fstat`/`fstatfs`/`close`, pauses after the relevant descriptor opens, and permits real rename/replacement of an opened ancestor or final pathname with a symlink/poison target; acquisition remains bound to retained descriptors and never accepts/follows replacement facts. A separate deterministic mount/device seam explicitly declares and inspects the full 7→9→7 script; the acquisition trace consumes only the 7→9 prefix, rejects at device 9, never opens the poison child, and never accepts the scripted final device-7 facts.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionRealAncestorReplacement|TestWalkerAcquisitionRealFinalReplacement|TestWalkerAcquisitionRealNoFollowBinding|TestWalkerAcquisitionMountPoisonScript)$' -count=1`.  
**Rollback boundary:** remove only these named tests and synchronization-only helpers, restoring the exact three-file baseline hashes above.  
**Independent status:** FAIL — verifier `subtask_gentle-ai-verify_1788117808925_f5d42ba5` rejected C1; its checked rows remain historical verification evidence only, exact accounting is superseded to **+233/-0**, and C1 satisfies no dependency.

- [x] **WU5C1C1 VERIFY:** Add the four exact named Darwin tests and compact helpers in `internal/platform/macos/walker_darwin_test.go`; use a real `t.TempDir()` component tree and real `openat`/`fstat`/`fstatfs`/`close` facts for synchronized ancestor/final rename-replacement, plus a separate scripted 7→9→7 mount-poison case that proves the poison child is never opened and final facts are never accepted. Run the focused command unchanged. Correct PASS is accepted verification; any correct failure stops for a separate strict RED → GREEN correction plan with no production edit in this unit. <!-- sdd-owner: implementation -->
- [x] **WU5C1C1 TRIANGULATE:** Exercise ancestor and final replacement independently with symlink and poison targets; assert retained-FD identity, no-follow flags, exact real opens/closes, no target facts, and no pathname-based fake ELOOP claim. Triangulate the separate device seam by asserting its complete scripted sequence is exactly 7→9→7, the acquisition trace consumes only 7→9, and work stops before poison-child open or final acceptance. <!-- sdd-owner: implementation -->
- [x] **WU5C1C1 REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin_test.go`, the exact focused command, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; record exact accounting/test hash, runtime N/A, rollback, and `cmp`/SHA-256 proof that both production walker files remain byte-identical before independent acceptance. <!-- sdd-owner: implementation -->

### WU5C1C1A — Correct Replacement Identity and Consumed Mount-Trace Verification

**Forecast:** 20–80 additions, 0 deletions, 20–80 authored changed lines; test-only and independently capped at 400.  
**Depends on:** independently accepted WU5C1B2B1A plus checked WU5C1C1 solely as a failed historical test baseline; C1 is not accepted and satisfies no dependency.  
**Allowed edit surface:** `internal/platform/macos/walker_darwin_test.go` only. Production, scan contract, enumeration, design, docs, commits, and reviews are out of scope.  
**Start:** verifier `subtask_gentle-ai-verify_1788117808925_f5d42ba5` rejected C1 despite all product/check commands otherwise passing. The exact C1 delta is **+233/-0**, superseding the receipt's +226/-0. Production hashes remain `walker_darwin.go` `beca553e58802696ba9af5d21281ef8707623d25d5ee92af302886e099ab644b` and `walker_other.go` `32a7f4dc2f588ac226b178b3cec700cb4309ca770eeec6c1fac418bbd05c8b72`; the failed C1 test hash is `eb8ce26e6974646ffe067834390b095c0764e1897ebab43d118d151d1c360b95`.  
**Finish:** the existing four C1 tests remain exact and focused. After each real ancestor/final rename, the fixture stats the renamed-original pathname and replacement target and fstats the retained descriptor/TrustedRoot evidence, then explicitly asserts retained identity equals renamed-original identity and differs from replacement-target identity. The mount poison test records consumed device values and asserts them exactly as `[7,9]`, asserts the declared script remains `[7,9,7]`, and explicitly proves the final `7` slot is unconsumed. No production behavior changes. A correct failure stops immediately for a separately planned genuine correction; it must not be bypassed or converted into acceptance.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionRealAncestorReplacement|TestWalkerAcquisitionRealFinalReplacement|TestWalkerAcquisitionRealNoFollowBinding|TestWalkerAcquisitionMountPoisonScript)$' -count=1`.  
**Rollback boundary:** remove only C1A's explicit post-rename identity comparisons and consumed-slot assertions, restoring failed C1 test hash `eb8ce26e6974646ffe067834390b095c0764e1897ebab43d118d151d1c360b95`; both production hashes remain unchanged.

- [x] **WU5C1C1A VERIFY:** In `internal/platform/macos/walker_darwin_test.go`, minimally correct the four existing C1 tests/helpers: after each ancestor/final rename-replacement, stat the renamed-original pathname and replacement target and fstat the retained pathname/FD evidence, explicitly asserting `TrustedRoot` retained identity equals the renamed original and differs from the replacement target; in `TestWalkerAcquisitionMountPoisonScript`, record and assert consumed devices exactly `[7,9]`, declared script exactly `[7,9,7]`, and final slot/index `7` explicitly unconsumed. Run the unchanged exact four-test focused command. If any genuine correction fails, stop with no production edit or acceptance. <!-- sdd-owner: implementation -->
- [x] **WU5C1C1A REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin_test.go`, the unchanged exact four-test focused command, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; record C1's superseded **+233/-0** accounting, exact C1A additions/deletions and final test hash, runtime N/A, rollback, production `cmp`/SHA-256 byte-identity, and independent acceptance before C2. <!-- sdd-owner: implementation -->

### WU5C1C2 — Verify Acquisition Checkpoint, Limit, and Lifecycle Matrices

**Forecast:** 120–240 additions, 0 deletions, 120–240 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5C1C1A. Checked failed WU5C1C1 and the historical failed WU5C1C candidate do not satisfy this dependency.  
**Allowed edit surface:** `internal/platform/macos/walker_darwin_test.go` only. Production walker files are read-only; do not duplicate already accepted B2B1A Cartesian coverage or add enumeration.  
**Start:** independently accepted C1A has corrected C1's real replacement identity comparisons and exact consumed/unconsumed 7→9→7 poison trace without production changes.  
**Finish:** exact named tests `TestWalkerAcquisitionCheckpointDescriptorLimitMatrix` and `TestWalkerAcquisitionRemainingLifecycleMatrix` cover every reachable acquisition checkpoint crossed with tight descriptor limits so cancellation and limit outcomes are distinguishable, then cover only the remaining non-accepted success/open/fstat/fstatfs/validation/handoff/close primary-secondary dimensions. The matrix asserts operation/path/class, exact opens/closes, no deeper work, nil capability on failure, successful handoff ownership, and no superficial duplicate cardinality.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionCheckpointDescriptorLimitMatrix|TestWalkerAcquisitionRemainingLifecycleMatrix|TestWalkerAcquisitionPrimaryCleanupCartesian|TestWalkerAcquisitionClosePrecedenceMatrix)$' -count=1`.  
**Rollback boundary:** remove only the two named C2 tests and their compact helpers, restoring the independently accepted C1 test hash while both production hashes remain unchanged.
**Independent status:** FAIL — verifier `subtask_gentle-ai-verify_1788119235934_81a16f3d` rejected C2; its checked rows remain historical verification evidence only, exact accounting is superseded to **+116/-0**, and C2 satisfies no dependency.

- [x] **WU5C1C2 VERIFY:** Add `TestWalkerAcquisitionCheckpointDescriptorLimitMatrix` and `TestWalkerAcquisitionRemainingLifecycleMatrix` in `internal/platform/macos/walker_darwin_test.go`; cross every reachable before/open/fstat/fstatfs/validation/handoff checkpoint with exact-boundary and one-too-tight descriptor limits, asserting whether cancellation or limit wins and proving the outcomes are not masked. Add only missing success/open/fstat/fstatfs/validation/handoff/close primary-secondary rows not already accepted. Correct PASS is accepted verification; any correct failure stops for a separate strict RED → GREEN correction plan with no production edit in this unit. <!-- sdd-owner: implementation -->
- [x] **WU5C1C2 TRIANGULATE:** Reuse existing trace/typed-error/close helpers to prove exact operation/path/class, open/close counts, no deeper operation, nil capability after failure, transferred ownership after success, primary-first context with discoverable secondaries, and distinct cancellation-versus-limit results; document exclusions for the 12 already accepted B2B1A Cartesian rows so cardinality is substantive rather than duplicated. <!-- sdd-owner: implementation -->
- [x] **WU5C1C2 REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin_test.go`, the exact focused command, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; record exact accounting/test hash, runtime N/A, rollback, and production `cmp`/hash byte-identity before independent acceptance authorizes WU5C2 enumeration. <!-- sdd-owner: implementation -->

### WU5C1C2A — Correct Trace, Open-Count, and Lifecycle-Inventory Verification

**Forecast:** 20–80 additions, 0 deletions, 20–80 authored changed lines; test-only and independently capped at 400.  
**Depends on:** independently accepted WU5C1C1A plus checked WU5C1C2 solely as a failed historical test baseline; C2 is not accepted and satisfies no dependency.  
**Allowed edit surface:** `internal/platform/macos/walker_darwin_test.go` only. Production, scan contract, enumeration, design, docs, commits, and reviews are out of scope.  
**Start:** verifier `subtask_gentle-ai-verify_1788119235934_81a16f3d` rejected C2 despite all product/check commands otherwise passing. Exact C2 accounting is **+116/-0**, superseding +112/-0. Production hashes remain `walker_darwin.go` `beca553e58802696ba9af5d21281ef8707623d25d5ee92af302886e099ab644b` and `walker_other.go` `32a7f4dc2f588ac226b178b3cec700cb4309ca770eeec6c1fac418bbd05c8b72`; failed C2 test hash is `43065a984ada9d107e954f970762d064bc0f80719c343fcf396a549c47fbc164`, with bounded evidence manifest `sha256:635b485e51986f9610a943d77eeb6d1f8423ce0ba44a3ae9cf83f328858013f3`.  
**Finish:** each reached-cancellation row in `TestWalkerAcquisitionCheckpointDescriptorLimitMatrix` explicitly asserts its exact operation trace, exact open count, immediate next operation absent, and immediate next open absent—not merely class or closes. The lifecycle inventory explicitly enumerates every primary-secondary dimension and maps each cell to accepted WU5C1B1A, WU5C1B2A, WU5C1B2B1A, or WU5C1C1A evidence, or to one of the two C2 lifecycle rows, proving no omission without broad comments. No production behavior changes. Any correct failure stops immediately with no production edit or acceptance and requires a separately planned genuine behavior correction.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerAcquisitionCheckpointDescriptorLimitMatrix|TestWalkerAcquisitionRemainingLifecycleMatrix|TestWalkerAcquisitionPrimaryCleanupCartesian|TestWalkerAcquisitionClosePrecedenceMatrix)$' -count=1`.  
**Rollback boundary:** remove only C2A's explicit per-row trace/open/no-deeper assertions and exhaustive inventory mapping, restoring failed C2 test hash `43065a984ada9d107e954f970762d064bc0f80719c343fcf396a549c47fbc164`; both production hashes remain unchanged.

- [x] **WU5C1C2A VERIFY:** In `internal/platform/macos/walker_darwin_test.go`, minimally correct the existing C2 tests/helpers so every reached-cancellation matrix row asserts the exact full trace and open count plus absence of the immediately deeper operation and open. Replace broad lifecycle-coverage comments with an explicit inventory of every primary-secondary dimension, mapping each cell to accepted WU5C1B1A/WU5C1B2A/WU5C1B2B1A/WU5C1C1A evidence or exactly one of C2's two new lifecycle rows and failing on any omitted or duplicate cell. Run the unchanged exact C2 focused command; if any genuine correction fails, stop with no production edit or acceptance. <!-- sdd-owner: implementation -->
- [x] **WU5C1C2A REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin_test.go`, the unchanged exact C2 focused command, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-macos-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-macos-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; record C2's superseded **+116/-0** accounting, exact C2A additions/deletions and final test hash, runtime N/A, rollback, production `cmp`/SHA-256 byte-identity, and independent acceptance before WU5C2. <!-- sdd-owner: implementation -->

### WU5C2A — Core Descriptor-Owned Enumeration (Historical Candidate; Independent FAIL)

**Actual accounting:** 274 additions, 2 deletions, **276 authored changed lines**; independently below 400.  
**Depends on:** independently accepted WU5C1C2A with exact hashes, production byte-identity, and rollback evidence; the blocked unsplit WU5C2 proposal and its four unchecked rows are superseded and satisfy no dependency.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go`, `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`. No scanner, finalizer, real rename/replacement fixture, lifecycle/depth-budget fixture, documentation, or generated surface.  
**Start:** all four allowed files match immutable baseline `/tmp/osdy-wu5c2-baseline-o82OM0` manifest `6e8a3785e9d5560c75680a57b314f70dafc8bf1ccc78890ff8214145ec41207b`; acquisition is accepted but cannot enumerate children.  
**Finish:** the scan-owned opaque contract can receive retained directory/skip/error facts but exposes no raw FD, `*os.File`, syscall flag, or capability; Darwin reads directory records only from the already-open FD through a narrow `getdirentries`/dirent seam, safely validates record lengths and names, ignores `.`/`..`, rejects slash/NUL/malformed records, sorts accepted names lexically, opens each child with `openat` against the exact parent and required flags, compares enumeration kind/inode with final-descriptor identity/kind, applies device/mount/local/type/identity gates before descent, and returns typed changed/boundary/invalid facts. The API shape must be sufficient for WU5C2B without implementing its real fixtures, depth/FD lifecycle, goroutines, content reads, `filepath.WalkDir`, or a generic traversal framework.  
**Focused command:** `go test ./internal/scan -run '^(TestDescriptorEnumerationFacts|TestDescriptorEnumerationOwnership)$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerDescriptorEnumeration|TestWalkerDirectoryRecords|TestWalkerChildOpenParentage|TestWalkerChildChanges|TestWalkerBoundaryGates)$' -count=1`.  
**Runtime harness evidence:** N/A — deterministic syscall/record seams are the bounded platform harness; no real lifecycle fixture belongs to A.  
**Rollback boundary:** restore the four exact baseline files/hashes from `/tmp/osdy-wu5c2-baseline-o82OM0`; this removes only A's contract, enumeration, gates, and tests and leaves accepted acquisition evidence intact.  
**Independent status:** FAIL — verifier `subtask_gentle-ai-verify_1788121542056_f459f57c` found that both parser and helper accepted a record without a NUL terminator; checked rows are historical evidence only and satisfy no dependency.

- [x] **WU5C2A RED:** In `internal/scan/descriptor_test.go` and `internal/platform/macos/walker_darwin_test.go`, add only the focused `DescriptorEnumerationFacts`, `DescriptorEnumerationOwnership`, `WalkerDescriptorEnumeration`, `WalkerDirectoryRecords`, `WalkerChildOpenParentage`, `WalkerChildChanges`, and `WalkerBoundaryGates` tests; assert opaque fact ownership with no descriptor/capability escape, already-open-FD read calls, malformed/short/overflowing record lengths, empty/dot/dotdot/slash/NUL names, lexical order, exact parent FD and `O_RDONLY|O_NOFOLLOW|O_DIRECTORY|O_CLOEXEC|O_NONBLOCK`, enumeration inode/kind versus final descriptor identity/kind, and device/mount/local/type/identity gates before descent; run the focused command and capture RED caused only by missing A APIs/behavior. <!-- sdd-owner: implementation -->
- [x] **WU5C2A GREEN:** In `internal/scan/descriptor.go` and `internal/platform/macos/walker_darwin.go`, minimally extend the scan-owned opaque fact contract and Darwin operation seam, parse and validate already-open-FD records, sort accepted names, open children relative to the exact parent with exact flags, derive accepted facts only from child `fstat`/`fstatfs`, and emit typed changed/boundary/invalid facts before any descent request; do not add real rename fixtures, depth/FD lifecycle, content reads, goroutines, `filepath.WalkDir`, or reusable framework layers; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C2A TRIANGULATE:** Extend only the same two test files with record-boundary and mixed-sibling tables, unavailable/zero enumeration inode, kind mismatch, final identity mismatch, symlink/special child, device/mount/non-local child, and combined isolated-error plus safe-sibling cases; prove malformed or changed children never descend, every safe sibling remains eligible in lexical order, and no raw FD/capability reaches scan callbacks; rerun the exact focused command and expect deterministic PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C2A REFACTOR:** Run `gofmt -w internal/scan/descriptor.go internal/scan/descriptor_test.go internal/platform/macos/walker_darwin.go internal/platform/macos/walker_darwin_test.go && go test ./internal/scan -run '^(TestDescriptorEnumerationFacts|TestDescriptorEnumerationOwnership)$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerDescriptorEnumeration|TestWalkerDirectoryRecords|TestWalkerChildOpenParentage|TestWalkerChildChanges|TestWalkerBoundaryGates)$' -count=1 && go test ./internal/scan ./internal/platform/macos -count=1 && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/scan/descriptor.go internal/scan/descriptor_test.go internal/platform/macos/walker_darwin.go internal/platform/macos/walker_darwin_test.go)"`; expect PASS, race N/A because A creates no goroutines, and record exact predecessor/final hashes, additions+deletions ≤400, runtime N/A, and rollback before independent A acceptance. <!-- sdd-owner: implementation -->

### WU5C2A1 — Require Terminated Darwin Directory Records (Accepted)

**Actual accounting:** 70 additions, 3 deletions, **73 authored changed lines**; independently below 400.  
**Depends on:** checked failed WU5C2A solely as the immutable correction baseline; A itself does not satisfy acceptance.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go` only. No scan contract, scanner, finalizer, B lifecycle/fixtures, design, docs, generated files, commit, or review surface.  
**Start:** failed A hashes are `walker_darwin.go` `d80e9955c7e261ba138d9e9a550ce212a9b14fbc2b81d9b45811f2e50917dba7` and `walker_darwin_test.go` `25ef09a08e0c3c3d5b82642f7c23218b49379d977e8ff8a2438ad37153133373`; A accounting is +274/-2 with no acceptance.  
**Finish:** a Darwin dirent is accepted only when `reclen >= nameOffset+namlen+1`, the byte exactly at `nameOffset+namlen` is NUL, and all existing bounds remain overflow-safe. Missing, non-NUL, truncated, or overflowing terminators are rejected before any child open; valid test helpers emit the terminator; existing embedded-NUL and slash rejection semantics remain unchanged. Final hashes are `walker_darwin.go` `412a412f0e2554d05f9198bdf6381a4fd75a08f7c01eb21f161ceefbc99de020` and `walker_darwin_test.go` `7809445656752727c40f8d4be6bbbdd11c8f78d3a91437d88930a7bcf9cf7d00`; semantic verifier `subtask_gentle-ai-verify_1788122606468_ea94f7a9` is PASS under user-authorized reset revision `sha256:bb7aa1149c06ca29e7b8ac7f20fb0a97cbf0291ef3c12c0f62dbec3ef20cce90`.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerDirectoryRecordTermination|TestWalkerDirectoryRecords)$' -count=1`.  
**Runtime harness evidence:** N/A — deterministic raw-record and child-open poison seams only.  
**Rollback boundary:** restore the two failed-A hashes above from `/tmp/osdy-wu5c2a1-baseline-VDEmju`, removing only A1 terminator validation/helper/tests while preserving the historical A candidate.

- [x] **WU5C2A1 RED:** In `internal/platform/macos/walker_darwin_test.go`, add exact `TestWalkerDirectoryRecordTermination` table cases for `reclen == nameOffset+namlen`, a non-NUL terminator, truncation before the terminator, and overflow/bounds failure; include a poison child-open seam and run the focused command, expecting genuine RED because malformed unterminated records are accepted or reach child open. <!-- sdd-owner: implementation -->
- [x] **WU5C2A1 GREEN:** In `internal/platform/macos/walker_darwin.go`, require overflow-safe `reclen >= nameOffset+namlen+1` and exact NUL at `nameOffset+namlen` before name acceptance or child open; in `walker_darwin_test.go`, make the valid dirent helper emit that exact terminator; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C2A1 TRIANGULATE:** Extend only `TestWalkerDirectoryRecordTermination` and existing `TestWalkerDirectoryRecords` with valid minimal and padded terminated records plus embedded-NUL and slash names; prove valid records retain lexical eligibility while missing/non-NUL/truncated/overflow records trigger zero child opens and embedded-NUL/slash semantics remain rejected; rerun the exact focused command and expect deterministic PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C2A1 REFACTOR:** Run `gofmt -w internal/platform/macos/walker_darwin.go internal/platform/macos/walker_darwin_test.go && go test ./internal/platform/macos -run '^(TestWalkerDirectoryRecordTermination|TestWalkerDirectoryRecords)$' -count=1 && go test ./internal/platform/macos -count=1 && go test ./... -count=1 && go vet ./... && test -z "$(gofmt -d internal/platform/macos/walker_darwin.go internal/platform/macos/walker_darwin_test.go)"`; PASS with corrected exact accounting +70/-3=73, final hashes `412a412f0e2554d05f9198bdf6381a4fd75a08f7c01eb21f161ceefbc99de020` and `7809445656752727c40f8d4be6bbbdd11c8f78d3a91437d88930a7bcf9cf7d00`, runtime N/A, exact rollback, and semantic verifier PASS before B1. <!-- sdd-owner: implementation -->

### WU5C2B — Combined Traversal Lifecycle and Adversarial Fixtures (Historical Untouched Oversized Plan; Non-Accepting)

**Historical forecast:** 220–330 production lines plus 330–480 named-test/fixture lines, **550–810 authored changed lines**; necessarily above 400.  
**Provenance:** before any Go/test write, baseline `/tmp/osdy-wu5c2b-baseline-kLCdoo` verified all six manifest entries; `SHA256SUMS` digest `e178b1cc4f02cf5d7c5727cb29500115800c0c98d4e2dca546e3f585e44c4ddf`. The oversized B reset and B1/B2 split use revision `sha256:21a23fbbca9599085cd4d6896f05d519a8da04b52176beae7cb4f43aa566ea45`. All four allowed Go/test files remained byte-identical: `descriptor.go` `eaef5480cbd0381649f1bfdfbc78708b868c1e3b92bff16a73e2a898b3b4c5ea`, `descriptor_test.go` `2cf87a50122416037ed60684b2d35d6bd9d82f2c7674bffd6ebef2e8cbe1a497`, `walker_darwin.go` `412a412f0e2554d05f9198bdf6381a4fd75a08f7c01eb21f161ceefbc99de020`, and `walker_darwin_test.go` `7809445656752727c40f8d4be6bbbdd11c8f78d3a91437d88930a7bcf9cf7d00`.  
**Disposition:** blocked before RED; no test, product, command, checkbox acceptance, commit, review, or lifecycle action occurred. These four rows are preserved unchecked only as historical non-executable context and satisfy no dependency.

- **WU5C2B HISTORICAL RED — DO NOT EXECUTE:** The combined RED would have mixed core DFS/FD/lifecycle seams with real rename/replacement fixtures and was blocked before any test write because the honest 550–810-line forecast exceeds 400; it is superseded by B1 then B2 and cannot be accepted. <!-- sdd-owner: implementation -->
- **WU5C2B HISTORICAL GREEN — DO NOT EXECUTE:** No combined production implementation exists; all Go/test files remained byte-identical to manifest `e178b1cc4f02cf5d7c5727cb29500115800c0c98d4e2dca546e3f585e44c4ddf`, and only independently accepted B1 may establish core DFS/lifecycle behavior. <!-- sdd-owner: implementation -->
- **WU5C2B HISTORICAL TRIANGULATE — DO NOT EXECUTE:** No combined real fixture or adversarial test was authored; independent B2 owns those fixtures only after accepted B1 and must not fabricate RED when B1 production already satisfies them. <!-- sdd-owner: implementation -->
- **WU5C2B HISTORICAL REFACTOR — DO NOT EXECUTE:** No combined verification, acceptance, rollback, commit, or review is permitted; retain this row as blocked provenance and use the separately bounded B1/B2 rollback boundaries below. <!-- sdd-owner: implementation -->

### WU5C2B1 — Core DFS, Descriptor Budget, and Lifecycle

**Forecast:** 240–360 additions, 20 deletions, **260–380 authored changed lines**; independently capped at 400.  
**Depends on:** accepted WU5C2A1 at exact +70/-3=73 and final hashes `412a412f0e2554d05f9198bdf6381a4fd75a08f7c01eb21f161ceefbc99de020` / `7809445656752727c40f8d4be6bbbdd11c8f78d3a91437d88930a7bcf9cf7d00`.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`; `internal/scan/descriptor.go` and `internal/scan/descriptor_test.go` only for a demonstrably minimal descriptor-error contract and its focused tests. No real pathname rename/replacement fixtures, scanner/finalizer, workers, goroutines, content reads, `filepath.WalkDir`, design, docs, commits, or review.  
**Start:** retain a fresh B1 baseline from `/tmp/osdy-wu5c2b-baseline-kLCdoo`; accepted A1 enumeration exists, while descendant DFS depth, pre-open FD capacity, full ownership ledger, and sibling continuation do not.  
**Finish:** serial DFS counts depth exactly below the acquired root; simultaneous descriptor capacity is checked before every child open; each capability has exact ownership and closes child-before-parent in LIFO order; operation errors remain primary with typed secondary close evidence, close is primary only after otherwise successful work, and isolated failures do not suppress safe siblings. Deterministic seams prove exact traces/counts without real rename fixtures.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerDFSDepth|TestWalkerDescriptorCapacity|TestWalkerDirectoryLifecycle|TestWalkerSiblingContinuation)$' -count=1`.  
**Rollback boundary:** restore the exact four A1/baseline Go/test hashes listed in historical B provenance, removing all B1 DFS/lifecycle seams, behavior, and tests while leaving A1 accepted.

- [x] **WU5C2B1 RED:** In `internal/platform/macos/walker_darwin_test.go` and only minimal `internal/scan/descriptor_test.go` contract cases if required, add deterministic tests first for root-relative depth zero/exact maximum/one-beyond, descriptor capacity exact-boundary/one-too-tight checked before child open, exact child-before-parent LIFO ownership, operation-primary plus typed secondary close evidence, close-primary after success, and safe later siblings; run the focused command and capture genuine failures caused only by missing B1 behavior. <!-- sdd-owner: implementation -->
- [x] **WU5C2B1 GREEN:** In `internal/platform/macos/walker_darwin.go` and only a minimal adjacent descriptor contract when unavoidable, implement serial DFS frames, exact below-root depth, pre-open descriptor reservation/release, one-owner LIFO cleanup, stable primary/typed-secondary precedence, and safe sibling continuation; rerun the focused command and expect PASS without real rename fixtures or B2-only seams. <!-- sdd-owner: implementation -->
- [x] **WU5C2B1 TRIANGULATE:** Extend deterministic seams with combined depth/FD boundaries, operation plus one/multiple close failures, close-after-success, malformed/changed child plus later safe siblings, and exact open/operation/close traces; prove no child opens without capacity, no double close, no masked primary, and no unsafe sibling suppression; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5C2B1 REFACTOR:** Run gofmt on only B1-touched walker/descriptor files, the exact B1 focused command, `go test ./internal/scan ./internal/platform/macos -count=1`, `go test ./... -count=1`, `go vet ./...`, and a clean `gofmt -d`; record exact baseline/final hashes, additions+deletions ≤400, runtime N/A, focused rollback, and independent acceptance before B2. <!-- sdd-owner: implementation -->

### WU5C2B2 — Adversarial Real Descriptor Fixtures

**Forecast:** 180–300 additions, 0 deletions, **180–300 authored changed lines**; independently capped at 400 and preferably test-only.  
**Depends on:** independently accepted WU5C2B1 with exact hashes, accounting, focused/full verification, and rollback.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin_test.go`; `internal/platform/macos/walker_darwin.go` only if a correct real-fixture expectation produces a genuine missing-behavior RED, and then only the minimum behavior correction. No scan contract, scanner/finalizer, worker, goroutine, content read, `filepath.WalkDir`, design, docs, commits, or review.  
**Start:** B1 core DFS/lifecycle behavior is accepted. Do not assume B2 needs production edits and do not fabricate RED.  
**Finish:** disposable `t.TempDir()` fixtures open a directory, rename it, install a replacement at the old pathname, and explicitly compare opened-original versus replacement identities while proving enumeration/child opens remain bound to the original descriptor. Vanished and symlink-replaced children produce changed evidence and no target descent; combined depth/FD and operation/close cases are added only where real fixtures expose a gap. Safe siblings continue.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerRealDirectoryReplacementBinding|TestWalkerRealChangedChildNoDescent|TestWalkerRealDescriptorAdversarialCombinations)$' -count=1`.  
**Rollback boundary:** remove B2-only real fixture tests and helpers to restore exact accepted-B1 hashes; if and only if B2 contains a genuine strict-TDD production correction, restore that minimal production delta separately to the accepted-B1 hash.

- [x] **WU5C2B2 VERIFY/RED:** Add the smallest real `t.TempDir()` opened-directory rename/replacement identity fixture and vanished/symlink-replaced child no-target-descent expectations in `walker_darwin_test.go`; run the focused command. If unchanged B1 production passes, record verification-first PASS with no RED claim or production edit; if a correct expectation fails, preserve that failure as genuine RED before any production change. <!-- sdd-owner: implementation -->
- [x] **WU5C2B2 GREEN/VERIFY:** When VERIFY passed, keep production byte-identical and add only test/helper evidence; when and only when the prior correct expectation produced genuine RED, make the minimum `walker_darwin.go` correction and rerun to GREEN. In both paths, explicitly stat/fstat and compare renamed-original versus replacement identities and prove child opens remain parent-descriptor-bound. <!-- sdd-owner: implementation -->
- [x] **WU5C2B2 TRIANGULATE:** Add vanished child, symlink replacement, no replacement-target descent, safe later sibling, and only-needed combined depth/FD plus operation/close real cases; assert exact identities, opens, closes, and traces, and prefer test-only coverage whenever accepted B1 behavior already satisfies the fixture. <!-- sdd-owner: implementation -->
- [x] **WU5C2B2 REFACTOR:** Run gofmt on B2-touched files, the exact B2 focused command, `go test ./internal/platform/macos -count=1`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; record whether production stayed byte-identical, exact hashes/accounting ≤400, fixture-local mutation only, rollback, and independent acceptance before WU5D1. <!-- sdd-owner: implementation -->

### WU5D1 — Consume the Walker for Serial Root Acquisition

**Forecast:** 240–320 additions, 0–20 deletions, 240–340 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5C2B2; failed A and the historical untouched oversized WU5C2B rows satisfy no dependency, and B1 alone is insufficient.  
**Allowed edit surfaces:** `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/scan/descriptor.go`.  
**Start:** platform traversal exists behind the scan-owned port, but no scanner consumer exists.  
**Finish:** an injected home resolver, fixed built-ins, and descriptor walker validate canonical roots before any platform call and process exactly five roots serially; missing/inaccessible/skipped acquisition states and cancellation-before-start are captured in five ordered transitional observations. Regular files are not consumed, and zero-file results are explicitly incomplete rather than scanned/complete.  
**Focused command:** `go test ./internal/scan -run '^(TestScannerCanonicalPreflight|TestScannerSerialRoots|TestScannerAcquisitionStates|TestScannerFiveTransitionalObservations|TestScannerNoRealHome)$' -count=1`.  
**Runtime harness evidence:** N/A — a fake descriptor walker and poison home seams are the bounded consumer harness.  
**Rollback boundary:** remove WU5D1-created scanner files and restore any minimal descriptor contract adjustment to independently accepted WU5C2B2 hashes.

- [x] **WU5D1 RED:** Add fake-walker tests for canonical built-in validation before platform construction/call, exact five-root serial order, normal/missing/inaccessible/symlink/non-directory/device-boundary/non-local acquisition states, cancellation before a root, poison resolver/platform seams, and five ordered honest incomplete zero-file transitional observations; run the focused command and capture RED caused only by missing scanner consumer APIs. <!-- sdd-owner: implementation -->
- [x] **WU5D1 GREEN:** Implement the minimum serial scanner consumer with injected home/built-ins/walker, validated absolute home and relative built-in components, one active root, stable acquisition-state mapping, later-root continuation, and five ordered transitional observations; do not add file workers or claim scanned/complete zero-byte coverage; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5D1 TRIANGULATE:** Add reordered fake definitions, duplicate/extra/unclean roots, root-level combined errors, cancellation after an earlier root, and poison callbacks; prove invalid canonical inputs make zero platform calls, safe later roots continue, no production home is read, and warnings/root states are deterministic; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5D1 REFACTOR:** Run `gofmt -w internal/scan/scanner.go internal/scan/scanner_test.go internal/scan/descriptor.go && go test ./internal/scan -run '^(TestScannerCanonicalPreflight|TestScannerSerialRoots|TestScannerAcquisitionStates|TestScannerFiveTransitionalObservations|TestScannerNoRealHome)$' -count=1 && go test ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; expect PASS, race N/A because roots remain serial without goroutines, and record predecessor/final hashes, <=400 accounting, runtime N/A, and rollback before WU5D2. <!-- sdd-owner: implementation -->

### WU5D2 — Map Directory Facts into Final Root Observations (Historical Independently Failed; Non-Accepting)

**Actual accounting:** `scanner.go` +114/-1, `scanner_test.go` +136/-0, `finalize.go` +7/-0, `finalize_test.go` +0/-0; **+257/-1=258 authored changed lines**, ≤400.  
**Depends on:** independently accepted WU5D1.  
**Allowed edit surfaces used:** `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/scan/finalize.go`, `internal/scan/finalize_test.go`.  
**Historical result:** verifier `subtask_gentle-ai-verify_1788140109519_c2f80e1f` found that production scanning never reaches directory facts because `Scanner` relies on a private fake-only type assertion, and that one-kind facts cannot distinguish enumerated kind from opened final kind. Preserve all four checked rows as failed/non-accepting history; D2 satisfies no dependency.  
**Final hashes:** `scanner.go` `bea7c0fa480b1eaa81cbe2c2faf597689ff278e0d2720ebb5a9b3d38fc274126`; `scanner_test.go` `7d4745598308b2e31b042ada221d45e1b26776079e7cd56f3d549ed1afc1289a`; `finalize.go` `2ffd49fcdd5011fe5012bdee31332468eeb2f0afc42a136c39ad8dfbc36c0479`; `finalize_test.go` `2ec604ae858ecad200e58a6ffc083898a35a0050c070fcc66fa500e614db3719`.  
**Rollback boundary:** restore the four files from `/tmp/osdy-wu5d2-baseline-vDTugL`; this removes only failed D2 while preserving WU5D1.

- [x] **WU5D2 RED:** Add fake-walker/finalizer tests for validated relative components and containment, synchronous directory kind/metadata/identity facts, enumeration-versus-open kind/identity mismatch, disappearance, symlink/special entry, different-device/non-local boundary, malformed/typed platform errors, deterministic warnings, safe siblings/later roots, and exact `partial > boundary_limited > scanned` mapping across all five final observations; run the focused command and capture RED caused only by missing directory-consumer integration. <!-- sdd-owner: implementation -->
- [x] **WU5D2 GREEN:** Map immutable walker directory/skip/error facts into raw scanner state and the existing finalizer, reject unclean/escaping components, classify changed/disappeared directories as zero-contribution `entry_changed`, retain lower-precedence warnings, continue safe siblings/later roots, and finalize five ordered observations without regular-file bytes; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5D2 TRIANGULATE:** Add combined changed/symlink/boundary/inaccessible fixtures and callback-order permutations; prove deterministic warning order, correct partial/boundary/root statuses, no target facts, no unsafe descent request, safe continuation, and no complete claim while regular-file evidence is absent; rerun and expect PASS without real-home inspection. <!-- sdd-owner: implementation -->
- [x] **WU5D2 REFACTOR:** Run `gofmt -w internal/scan/scanner.go internal/scan/scanner_test.go internal/scan/finalize.go internal/scan/finalize_test.go && go test ./internal/scan -run '^(TestScannerDirectoryFacts|TestScannerDirectoryChanges|TestScannerDirectoryBoundaries|TestScannerRootStatusMapping|TestScannerFiveFinalObservations)$' -count=1 && go test ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; expect PASS, race N/A because no goroutines exist, and record accepted-WU5D1/final hashes, <=400 accounting, runtime N/A, and rollback before WU5E1. <!-- sdd-owner: implementation -->

### WU5D2A — Correct Production Fact Traversal and Kind-Pair Semantics (Historical Independently Failed; Non-Accepting)

**Actual accounting:** `descriptor.go +41/-0`, `descriptor_test.go +4/-0`, `scanner.go +24/-36`, `scanner_test.go +58/-3`, `walker_darwin.go +48/-4`, `walker_darwin_test.go +17/-0`, `walker_other.go +4/-0`; **+196/-43=239 authored changed lines**, ≤400.  
**Depends on:** independently accepted WU5D1 plus checked failed WU5D2 solely as its immutable correction baseline; D2 is not accepted and satisfies no dependency.  
**Historical result:** verifier `subtask_gentle-ai-verify_1788141318866_d33bc6e5` found that production `WalkFacts` is a separate single-level enumeration beside recursive `walkDirectory`, omits nested facts, and permits duplicate traversal. Preserve all four checked rows as failed/non-accepting history; D2A satisfies no dependency.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go`, `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`; use `walker_other.go` only if the public interface requires a minimum typed-unsupported compiling method. Do not edit finalizer, design, product docs, generated files, or lifecycle surfaces.  
**Start:** failed D2 exact hashes above; unchanged descriptor/platform hashes are `descriptor.go` `6f7741457a761c95e0532e271394883b3a6b6eab64343d8479ca8e4b09f14a5b`, `descriptor_test.go` `2cf87a50122416037ed60684b2d35d6bd9d82f2c7674bffd6ebef2e8cbe1a497`, `walker_darwin.go` `4c4439d46c44dc88f587a5f22e45944b0d954496b84a3bb115ae05b7995092e4`, `walker_darwin_test.go` `3f4d9e17ca2d31a4e205058a597859385eb2ca760d8633b31c8992534b242683`, and `walker_other.go` `32a7f4dc2f588ac226b178b3cec700cb4309ca770eeec6c1fac418bbd05c8b72`.  
**Finish:** the scan-owned public `DescriptorWalker` contract has one minimal opaque synchronous fact/event callback port used identically by fake and Darwin production walkers; facts preserve enumerated and opened-final kind/identity separately; any kind or identity change/disappearance maps to zero-contribution `entry_changed`, while stable original symlink/special entries retain `symlink_skipped`/`unsupported_entry_type` and existing device/nonlocal/malformed/error semantics. Five roots traverse facts exactly once after acquisition, keep trusted-root ownership through traversal and one close, continue safe siblings/later roots, expose no raw FD, and create no regular-file work, goroutine, or content read.  
**Focused commands:** `go test ./internal/scan -run '^(TestScannerProductionFactPort|TestScannerFactKindIdentityPairs|TestScannerStableSkippedKinds|TestScannerFiveProductionShapedTraversals)$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerFactPortTraversal|TestWalkerFactPortOwnership|TestWalkerFactPortKindIdentityPairs|TestWalkerFactPortNoDuplicateTraversal)$' -count=1`.  
**Runtime harness evidence:** N/A — production-shaped scan adapters plus deterministic Darwin descriptor seams are the bounded synchronous harness; no CLI or regular-file pipeline exists.  
**Rollback boundary:** restore exactly the four failed-D2 hashes and five unchanged descriptor/platform hashes listed above; remove only D2A contract/adapter tests and behavior, leaving failed D2 historical evidence intact.

- [x] **WU5D2A RED:** In the allowed scan/platform test files, add production-shaped adapter tests proving the public fact port is required on both fake and Darwin walkers, five acquired roots invoke it synchronously exactly once before one retained-root close, and facts carry separate enumerated/opened kind+identity; require changed/disappeared kind or identity to emit zero-contribution `entry_changed`, stable symlink/special to remain skipped/unsupported, preserved boundary/error semantics, safe continuation, no raw FD/content/regular-file/goroutine work, then run both focused commands and capture genuine RED because current production lacks the port and kind-pair model. <!-- sdd-owner: implementation -->
- [x] **WU5D2A GREEN:** In `descriptor.go`, `scanner.go`, and Darwin walker production, add the minimum scan-owned opaque synchronous callback method to `DescriptorWalker`, implement it over existing descriptor-owned traversal without duplicate enumeration, and map explicit enumerated/opened kind+identity pairs with changed/disappeared zero contribution; add only the minimum `walker_other.go` typed-unsupported method if interface compilation requires it, then rerun both focused commands to GREEN. <!-- sdd-owner: implementation -->
- [x] **WU5D2A TRIANGULATE:** Extend scan and Darwin tables across stable directory/symlink/special versus kind-changed, identity-changed, disappeared, device/nonlocal, malformed, and scoped-error matrices; prove exact synchronous callback/close order for all five roots, no duplicate traversal or FD leak, safe siblings/later roots, deterministic warnings, and no regular-file/content/goroutine behavior; rerun both focused commands and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5D2A REFACTOR:** Run `gofmt` only on touched allowed files, both focused commands, `go test ./internal/scan -count=1`, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-wu5d2a-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-wu5d2a-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; record genuine RED/GREEN evidence, exact +/− ≤400, final hashes, synchronous ownership/no-leak evidence, runtime N/A, and rollback, then require independent PASS before WU5E1. <!-- sdd-owner: implementation -->

### WU5D2A1 — Unify Darwin Recursive Traversal and Fact Emission

**Accepted accounting:** 270 additions, 97 deletions, **367 authored changed lines** from canonical baseline `/tmp/osdy-wu5d2a1-baseline-zGhBuB`; independently verified within the 400-line cap.  
**Acceptance:** verifier `subtask_gentle-ai-verify_1788143365237_f68de7e6` passed functional/source/accounting checks; parent-native lens mode `all` reported zero blockers with one unchanged dispositioned false-positive warning, LSP reported zero errors, and adjudicator `subtask_gentle-ai-verify_1788143523521_bbd9b818` returned strict PASS.  
**Depends on:** independently accepted WU5D1 plus checked failed WU5D2A solely as the immutable correction baseline; failed D2 and D2A satisfy no dependency.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go`, `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`; `internal/platform/macos/walker_other.go` only if the public interface needs a minimum typed-unsupported compile adjustment. Do not edit finalizer, WU5E1/file-pipeline behavior, design, specs, proposal, docs, generated files, or lifecycle surfaces.  
**Start:** failed D2A final hashes from its receipt are the immutable baseline; its public `WalkFacts` performs a separate single-level root enumeration while legacy `walkDirectory` owns recursive DFS.  
**Finish:** one Darwin recursive engine synchronously emits `scan.DirectoryFact` for the accepted root, every accepted descendant directory, and changed/disappeared/boundary events. Public `WalkFacts` is the scanner entry and invokes that engine once; private legacy wrappers may delegate to the same engine only for existing tests and never cause a second traversal in one scan. The engine preserves accepted B1/B2 deterministic DFS depth semantics, simultaneous FD capacity, child-before-parent LIFO closes, primary-error precedence with secondary close evidence, descriptor-bound rename/replacement safety, safe-sibling continuation, one retained-root close, no raw FD exposure, and no regular-file/content/goroutine work.  
**Focused commands:** `go test ./internal/scan -run '^(TestScannerProductionFactPort|TestScannerFactKindIdentityPairs|TestScannerStableSkippedKinds|TestScannerFiveProductionShapedTraversals)$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerFactPortTraversal|TestWalkerFactPortNestedFacts|TestWalkerFactPortOwnership|TestWalkerFactPortKindIdentityPairs|TestWalkerFactPortNoDuplicateTraversal)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized scanner and Darwin descriptor seams are the bounded recursive ownership harness; no CLI or regular-file pipeline exists.  
**Rollback boundary:** restore the seven exact failed-D2A product/test hashes recorded in `apply-progress.md`, removing only D2A1 unified-recursion behavior and tests; failed D2A remains historical evidence and WU5E1 stays blocked.

- [x] **WU5D2A1 RED:** In `internal/scan/scanner_test.go` add the exact missing tests `TestScannerStableSkippedKinds` and `TestScannerFiveProductionShapedTraversals`; in `internal/platform/macos/walker_darwin_test.go` add `TestWalkerFactPortNestedFacts`, `TestWalkerFactPortOwnership`, `TestWalkerFactPortKindIdentityPairs`, and `TestWalkerFactPortNoDuplicateTraversal`. Require nested accepted-directory facts plus changed/disappeared/boundary events from one public `WalkFacts` call, exactly one retained-root close, no raw FD, and no second enumeration; run both focused commands and capture genuine current RED from missing nested/no-duplicate behavior rather than missing test symbols or a fabricated oracle. <!-- sdd-owner: implementation -->
- [x] **WU5D2A1 GREEN:** In `walker_darwin.go`, replace the separate single-level `WalkFacts` path with one recursive descriptor-owned engine that emits copied `scan.DirectoryFact` values synchronously for root, accepted descendants, and changed/disappeared/boundary outcomes; make public `WalkFacts` the scanner entry and let any test-only private legacy wrapper delegate once to that same engine. Preserve scanner mapping in `descriptor.go`/`scanner.go`, B1/B2 DFS depth and FD-capacity gates, LIFO close ownership, primary-error precedence, replacement safety, and zero regular-file/content/goroutine work; rerun both focused commands to GREEN. <!-- sdd-owner: implementation -->
- [x] **WU5D2A1 TRIANGULATE:** Extend the named scan/Darwin tables across multi-level lexical DFS, stable directory/symlink/special kind-identity pairs, changed kind, changed identity, disappearance, device/nonlocal boundary, malformed/scoped error, safe later sibling, depth/FD exact limits, close-primary/secondary combinations, and pathname replacement. Assert each accepted descriptor closes exactly once child-before-parent, root closes once after callbacks, each directory is enumerated once, no duplicate fact/path or raw FD escapes, and scanner warnings remain deterministic across five production-shaped roots. <!-- sdd-owner: implementation -->
- [x] **WU5D2A1 REFACTOR:** In `internal/scan/scanner_test.go`, replace the panic-based `scannerDefinitions(home)` helper with `scannerDefinitions(t, home)`, call `t.Helper()`, report construction failure with `t.Fatal`/`t.Fatalf`, and update every caller; in production `internal/scan/scanner.go`, remove the now-unused `transitional` helper. Then run `gofmt` only on touched allowed files, both focused commands, the accepted B1/B2 DFS/lifecycle/replacement focused tests, `go test ./internal/scan -count=1`, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-wu5d2a1-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-wu5d2a1-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`. Run a fresh `lens_diagnostics` with `mode=all` over every A1-touched file after those cleanups; unrelated false-positive N+1/context advisories in tests are nonblocking, but acceptance requires zero blocker diagnostics and zero LSP errors. Record genuine RED/GREEN evidence, exact +/− ≤400, all final hashes, one-engine/no-duplicate/close-once evidence, runtime N/A, and rollback, then require independent PASS before WU5E1. <!-- sdd-owner: implementation -->

### WU5E1 — Add Bounded Regular-File Jobs and Safe Relative Reopen (Historical Untouched Oversized; Nonexecuting)

**Forecast:** detailed blocked forecast **600–925 authored changed lines**; this combined unit is not independently executable.  
**Depends on:** independently accepted WU5D2A1; checked failed WU5D2 and WU5D2A are insufficient.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`.  
**Start:** independently accepted WU5D2A1 unified recursive fact traversal passes with no regular-file facts or goroutines.  
**Finish:** historical combined target only; WU5E1A and WU5E1B now own its implementation sequentially.  
**Focused command:** historical combined command only; do not execute these rows.  
**Runtime harness evidence:** N/A — the workload gate stopped before RED or any product/test command.  
**Baseline/provenance:** canonical untouched baseline `/tmp/osdy-wu5e1-baseline-tRW0me`; `SHA256SUMS` SHA-256 `7db22a09131ec600ccefcf78d06a30be8384bfe2a7114925c21834d275f1de26`; all seven entries and five WU5E1 source/test files were verified byte-identical before the blocked forecast. No product/test change occurred.  
**Rollback boundary:** none required for the blocked combined unit; no product/test edit exists to roll back. Preserve the accepted WU5D2A1 boundary and selectively staged seven-file index state.

- **WU5E1 HISTORICAL RED — DO NOT EXECUTE:** Add synchronized scanner and Darwin tests for fixed worker/job/result counts, bounded capacities and backpressure, validated relative identity-chain jobs, trusted-root private-anchor reopen, rolling ancestor descriptors, exact no-follow flags, ancestor/final identity and device/local checks, enumeration-versus-open kind/identity/disappearance, regular-only logical/allocation facts, no content reads, and exact close counts; run both focused commands and capture RED caused only by the missing file pipeline/reopen APIs. <!-- sdd-owner: implementation -->
- **WU5E1 HISTORICAL GREEN — DO NOT EXECUTE:** Implement the minimum fixed worker and bounded channel pipeline plus platform relative reopen: stop absolute descendant inspection, reopen each component from the private root anchor, close rolling descriptors, accept only unchanged same-boundary regular final-descriptor facts, and return changed/boundary/error results with zero bytes otherwise; rerun both focused commands and expect PASS. <!-- sdd-owner: implementation -->
- **WU5E1 HISTORICAL TRIANGULATE — DO NOT EXECUTE:** Add blocked enqueue/result cases, ancestor/final rename or symlink replacement, disappearance, non-regular entries, different-device/non-local facts, metadata/close failures, deep identity chains, and queue-pressure permutations; prove bounded goroutines/FDs, no target/content facts, no dropped accepted result, and deterministic zero contribution for changed entries; rerun the focused commands and `go test -race ./internal/scan -run '^(TestScannerBoundedFileJobs|TestScannerFileBackpressure|TestScannerRegularOnlyFacts|TestScannerFileEntryChanges)$' -count=1`, expecting PASS. <!-- sdd-owner: implementation -->
- **WU5E1 HISTORICAL REFACTOR — DO NOT EXECUTE:** Run `gofmt -w internal/scan/descriptor.go internal/scan/scanner.go internal/scan/scanner_test.go internal/platform/macos/walker_darwin.go internal/platform/macos/walker_darwin_test.go && go test ./internal/scan -run '^(TestScannerBoundedFileJobs|TestScannerFileBackpressure|TestScannerRegularOnlyFacts|TestScannerFileEntryChanges)$' -count=1 && go test ./internal/platform/macos -run '^(TestWalkerRelativeFileReopen|TestWalkerFileIdentityChain|TestWalkerRegularFileFacts|TestWalkerFileReopenLifecycle)$' -count=1 && go test -race ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; expect PASS and record exact predecessor/final hashes, <=400 accounting, runtime N/A, and rollback before WU5E2. <!-- sdd-owner: implementation -->

### WU5E1A — Establish the Scanner-Owned Bounded File Pipeline and Contract

**Forecast:** 260–360 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5D2A1.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go`, `internal/scan/scanner.go`, `internal/scan/scanner_test.go`. Fakes only: do not edit Darwin files or access a real filesystem.  
**Start:** accepted D2A1 emits recursive directory facts synchronously, but no scan-owned regular-file job/result port or bounded file pipeline exists.  
**Finish:** scan owns opaque validated `FileJob` and `FileResult` values; jobs contain copied relative components plus accepted ancestor and final identities, never an absolute path, raw FD, or platform handle. A fixed worker count and bounded job/result channels enforce backpressure; local cancellation/close ownership stops acceptance, closes each channel exactly once, drains accepted results, joins workers, and leaks no goroutine. Only validated regular-file facts aggregate, sorted deterministically independent of fake completion order. Hard-link attribution, cross-root completion permutations, and snapshot cancellation/status integration remain exclusively WU5E2.  
**Focused command:** `go test ./internal/scan -run '^(TestScannerBoundedFileJobs|TestScannerFileBackpressure|TestScannerRegularOnlyFacts|TestScannerFileEntryChanges)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized fake file-port/channel seams are the bounded concurrency harness; no Darwin implementation or real filesystem fixture is allowed.  
**Rollback boundary:** restore the exact independently accepted WU5D2A1 versions of the four allowed scan files; this removes only E1A contract/pipeline behavior and leaves E1B/E2 absent.  
**Acceptance:** independent verifier `subtask_gentle-ai-verify_1788146646060_00f9245a` PASS; exact baseline delta **+400/-0=400**; final SHA-256 values are `descriptor.go` `aa191d41ad70864d3bc7ca954fbe8f6c31f7970693dfe7573460d17a5616d620`, `descriptor_test.go` `f2ebb0847e76c497986efc234d642596b75f870875cfa1d5bc306a126cc15315`, `scanner.go` `672f14c0e410c9aef07b5eacadec53f6b8524e484a8da266c43c87f40342deb6`, and `scanner_test.go` `6abfa3912d62446a74f207f227832379c79c6df9c92134cdf152bab55acd6417`; parent LSP reported 0 errors, parent lens diagnostics were clean, and settlement state is `complete`. The seven D2A1 files remain staged while E1A working changes remain unstaged; no commit or push exists.

- [x] **WU5E1A RED:** In `internal/scan/descriptor_test.go` and `internal/scan/scanner_test.go`, add the exact existing tests `TestScannerBoundedFileJobs`, `TestScannerFileBackpressure`, `TestScannerRegularOnlyFacts`, and `TestScannerFileEntryChanges` using only synchronized fakes. Require opaque constructor-validated jobs/results, copied relative components and ancestor/final identities, rejection of absolute/unclean/NUL components and raw capability exposure, exact fixed worker and bounded channel counts, observable job/result backpressure, regular-only aggregation, changed/error zero contribution, and deterministic path ordering; add a lifecycle case that cancels/closes/releases blocked fake work and proves exact close/join counts with no leak, then run the focused command and capture RED caused only by missing E1A APIs. <!-- sdd-owner: implementation -->
- [x] **WU5E1A GREEN:** In `internal/scan/descriptor.go` and `internal/scan/scanner.go`, implement the minimum opaque validated `FileJob`/`FileResult` port and scanner-owned fixed-worker bounded job/result pipeline; copy relative components and identities, apply backpressure, close inputs/results exactly once, drain accepted results, join workers, map only unchanged regular facts, and sort retained facts by normalized relative path while keeping absolute paths, raw FDs, Darwin syscalls, real filesystem access, hard-link attribution, and snapshot cancellation/status integration absent; rerun the focused command and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5E1A TRIANGULATE:** Extend the four exact scanner tests with zero/one/full-capacity queues, blocked producer and blocked result consumer, cancellation before acceptance and during fake work, repeated close attempts, completion-order permutations, malformed identity chains, changed final identity/kind, nonregular results, fake errors, and race-synchronized release; prove bounded goroutines, no post-close acceptance, no dropped accepted result, no leak, regular-only zero-safe facts, and deterministic ordering, then run the focused command and `go test -race ./internal/scan -run '^(TestScannerBoundedFileJobs|TestScannerFileBackpressure|TestScannerRegularOnlyFacts|TestScannerFileEntryChanges)$' -count=1`. <!-- sdd-owner: implementation -->
- [x] **WU5E1A REFACTOR:** Run `gofmt -w internal/scan/descriptor.go internal/scan/descriptor_test.go internal/scan/scanner.go internal/scan/scanner_test.go && go test ./internal/scan -run '^(TestScannerBoundedFileJobs|TestScannerFileBackpressure|TestScannerRegularOnlyFacts|TestScannerFileEntryChanges)$' -count=1 && go test -race ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; expect PASS, verify source contains no Darwin syscall/build-tag/raw-FD/absolute-descendant or real-filesystem implementation, and record exact accepted-D2A1 predecessor/final hashes, additions+deletions ≤400, runtime N/A, lifecycle/no-leak evidence, and rollback before E1B. <!-- sdd-owner: implementation -->

### WU5E1B — Implement Darwin Private-Root Relative File Reopen

**Forecast:** 280–390 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5E1A.  
**Allowed edit surfaces:** `internal/platform/macos/walker_darwin.go`, `internal/platform/macos/walker_darwin_test.go`; `internal/scan/descriptor.go` only for a minimum contract correction that remains within E1A's opaque port, and `internal/scan/scanner.go` only for minimal adapter wiring. No finalizer, hard-link, broad cancellation/status, report, CLI, or documentation edit.  
**Start:** accepted E1A supplies the bounded opaque file-job/result port through fakes only; Darwin does not implement relative file reopen.  
**Finish:** Darwin implements the E1A port from a private retained root anchor. It rolls `openat` component-by-component with read-only, no-follow, close-on-exec, and nonblocking flags; validates every ancestor and the final descriptor with `fstat`/`fstatfs` for expected identity, device, mount/local boundary, and regular type; exposes checked logical/allocation metadata only for regular files; never reads content; and closes superseded/final descriptors exactly once with operation-primary and cleanup-secondary precedence. Scanner integration is the minimum required to select this adapter; E2 hard links, cancellation integration, and scheduling determinism remain untouched.  
**Focused command:** `go test ./internal/platform/macos -run '^(TestWalkerRelativeFileReopen|TestWalkerFileIdentityChain|TestWalkerRegularFileFacts|TestWalkerFileReopenLifecycle)$' -count=1`.  
**Runtime harness evidence:** N/A — Darwin `t.TempDir()` rename/symlink/disappearance/nonregular/boundary fixtures and narrow descriptor seams are the bounded platform harness.  
**Rollback boundary:** restore accepted E1A scan hashes and accepted D2A1 Darwin hashes, removing only E1B reopen implementation, platform fixtures, and minimal adapter wiring; E2 remains absent.

- [x] **WU5E1B RED:** In Darwin-tagged `internal/platform/macos/walker_darwin_test.go`, add the exact platform tests `TestWalkerRelativeFileReopen`, `TestWalkerFileIdentityChain`, `TestWalkerRegularFileFacts`, and `TestWalkerFileReopenLifecycle`. Use real `t.TempDir()` files plus synchronized rename, ancestor/final symlink replacement, disappearance, directory/FIFO/socket-like nonregular, and controlled boundary fixtures to require a private retained root anchor, rolling parent-relative opens with exact read-only/no-follow/cloexec/nonblock flags, ancestor/final identity and `fstat`/`fstatfs` device/mount/local checks, regular-only logical/allocation facts, no content read, and exact close/error precedence; run the focused command and capture RED caused only by missing E1B behavior. <!-- sdd-owner: implementation -->
- [x] **WU5E1B GREEN:** In `internal/platform/macos/walker_darwin.go`, implement the E1A file port with a private retained root anchor and rolling `openat` acquisition; validate each ancestor before opening the next component, validate final identity/boundary/locality/regular type from `fstat`/`fstatfs`, compute checked logical size and allocation only for regular files, return changed/boundary/scoped-error zero-byte results otherwise, close each superseded/final descriptor exactly once, and preserve primary operation errors over close errors without reading content or exposing descriptors; add only minimal scanner adapter wiring if compilation requires it, then rerun the focused command to GREEN. <!-- sdd-owner: implementation -->
- [x] **WU5E1B TRIANGULATE:** Extend the four exact platform tests across ancestor and final rename/replacement identities, symlink insertion, disappearance before each open, nonregular final descriptors, device/mount/nonlocal boundary changes, invalid/overflow metadata, open/fstat/fstatfs/close failures, and operation-primary × cleanup-secondary matrices; assert no target/content fact, no absolute descendant reopen, constant rolling worker FD use, exact close counts/order, safe zero contribution, and unchanged E1A bounded-pipeline behavior, then rerun the focused command plus the E1A focused and race commands. <!-- sdd-owner: implementation -->
- [x] **WU5E1B REFACTOR:** Run `gofmt` only on touched E1B allowed files, the exact platform focused command, the exact E1A scanner focused command, `go test -race ./internal/scan -count=1`, `go test ./internal/platform/macos -count=1`, `GOOS=linux go test -c -o /tmp/osdycleaner-wu5e1b-linux.test ./internal/platform/macos && rm -f /tmp/osdycleaner-wu5e1b-linux.test`, `go test ./... -count=1`, `go vet ./...`, and clean `gofmt -d`; expect PASS and record accepted-E1A/D2A1 predecessor and final hashes, additions+deletions ≤400, real-fixture identity/boundary/no-content evidence, exact lifecycle/error precedence, runtime N/A, and rollback before E2. <!-- sdd-owner: implementation -->

### WU5E1B1 — Scanner FilePort Wiring Correction Plan

**Forecast:** 180–320 additions, 0–40 deletions, 180–360 authored changed lines; independently capped at 400.
**Depends on:** accepted WU5E1A; WU5E1B is historical wiring-incomplete evidence and cannot satisfy this dependency.
**Allowed edit surfaces:** `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, plus append-only OpenSpec evidence. No Darwin, descriptor contract, finalizer, report, CLI, index, or lifecycle edit.
**Finish:** scanner dynamically selects `FilePort` from the acquired root, turns accepted regular `DirectoryFact` identity chains into relative `FileJob`s, submits them to one bounded pipeline, closes/drains it, consumes accepted results deterministically, and calls `root.Close` only afterward; changed/missing/nonregular facts produce no job or bytes. No absolute descendant path, raw FD, or content read is introduced.
**Focused command:** `go test ./internal/scan -run '^TestScannerFilePortWiring$' -count=1`.
**Runtime harness evidence:** N/A — a production-shaped retained-root/file-port fake with synchronized events is the bounded scanner harness.
**Rollback boundary:** restore the accepted E1B scanner files; this removes only scanner wiring and its integration test.

- [x] **WU5E1B1 RED:** Add `TestScannerFilePortWiring` first with a production-shaped retained root that also implements `FilePort`; require dynamic selection, accepted-directory ancestor/final identity-chain job construction, changed/missing/nonregular no-job cases, bounded submit/close/drain, accepted-result consumption, deterministic result order, and an event trace proving pipeline completion precedes root close. <!-- sdd-owner: implementation -->
- [x] **WU5E1B1 GREEN:** Wire `consumeDirectories` to select the root's `FilePort`, collect accepted regular facts into validated relative jobs, submit/close/drain one bounded pipeline, and consume only accepted results before `root.Close`, without absolute descendants, raw FDs, or content reads. <!-- sdd-owner: implementation -->
- [x] **WU5E1B1 TRIANGULATE:** Extend the integration table across reordered facts/results, changed identity/kind, missing/error, and submit/result pressure; prove no accepted result is dropped, no invalid fact is submitted, order is stable, and close cannot race ahead of drain. <!-- sdd-owner: implementation -->
- [x] **WU5E1B1 REFACTOR:** Run `gofmt -w internal/scan/scanner.go internal/scan/scanner_test.go && go test ./internal/scan -run '^TestScannerFilePortWiring$' -count=1 && go test -race ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; record RED/GREEN/TRIANGULATE evidence, exact <=400 accounting, hashes, runtime N/A, rollback, and independently accepted E1B1 before E2. <!-- sdd-owner: implementation -->

### WU5E1B2 — Scanner-Owned Accepted-Result Retention

**Scope:** only `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, and append-only evidence; <=400 lines; defer hard links, cross-root finalization, and broad cancellation/status work.

- [x] **WU5E1B2 RED:** Add a production-shaped, context-honoring root test proving scanner-owned state retains normalized accepted `FileResult`s deterministically before `root.Close`; rejected results map through existing warnings. <!-- sdd-owner: implementation -->
- [x] **WU5E1B2 GREEN:** Replace the fake-only private `fileResultConsumer` seam with scanner-owned accepted-result retention before root close, preserving existing rejected-result warning classification. <!-- sdd-owner: implementation -->
- [x] **WU5E1B2 TRIANGULATE:** Prove ordered accepted retention and changed/missing/nonregular rejection under context honoring without FilePort-only fake observation. <!-- sdd-owner: implementation -->
- [x] **WU5E1B2 REFACTOR:** Run focused scanner, race, full, vet, and gofmt checks; record strict RED→GREEN→TRIANGULATE→REFACTOR evidence, <=400 accounting, and rollback. <!-- sdd-owner: implementation -->

### WU5E2 — Finalize Pipeline Cancellation, Determinism, and Hard Links

**Forecast:** 230–300 additions, 10–30 deletions, 240–330 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5E1B2.
**Allowed edit surfaces:** `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/scan/finalize.go`, `internal/scan/finalize_test.go`.  
**Start:** E1A's bounded pipeline and E1B's Darwin reopen return regular-file facts, but scan-wide cancellation state integration, completion-order determinism, and hard-link attribution evidence are absent.  
**Finish:** scan-wide cancellation stops new jobs, drains accepted results, joins all fixed workers, preserves deterministic warning/fact order across completion permutations, delegates same- and cross-area hard-link attribution to the finalizer, leaks no goroutine or descriptor, and permits WU6 only after independent PASS. Do not reopen or duplicate E1A queue/contract tests or E1B Darwin pathname/identity/lifecycle fixtures.  
**Focused command:** `go test ./internal/scan -run '^(TestScannerFileCompletionPermutations|TestScannerHardLinkFinalization|TestScannerCancellationStopDrainJoin|TestScannerNoPipelineLeaks|TestScannerDeterministicWarnings)$' -count=1`.  
**Runtime harness evidence:** N/A — synchronized channel fixtures are the bounded scan-wide cancellation/determinism harness; no CLI exists.  
**Rollback boundary:** restore accepted WU5E1B2 scanner hashes `scanner.go` `38c5f6288da83c40175a363769c612d442f5e3be23884726db593022e50ca1cf` and `scanner_test.go` `4167f5789e76cc6e120e2ddf628f50f69b939f40f7e56322f340952943e837ca`, removing only WU5E2 scan-wide cancellation, aggregation, and hard-link finalizer integration.

- [x] **WU5E2 RED:** Add channel-synchronized tests for worker/result completion permutations, same-identity hard links, changed/disappeared file warnings, cancellation before enqueue/after acceptance/while blocked, stopped new jobs, result draining, worker joining, no leaked goroutines, and deterministic finalizer attribution/warnings; run the focused command and capture RED caused only by missing completion/cancellation integration. <!-- sdd-owner: implementation -->
- [x] **WU5E2 GREEN:** Implement cancellation-aware ownership that stops enqueueing and worker acceptance, closes job input once, drains every accepted result, joins the fixed workers, canonically sorts facts before existing hard-link finalization, and preserves completed roots plus active/later cancellation states without scheduling-dependent attribution; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU5E2 TRIANGULATE:** Add blocked job/result channels, repeated cancellation, mixed boundary/partial/change errors, worker completion permutations, cross-area hard links, and close/error combinations; prove cancellation precedence, no discarded accepted fact, no post-cancel open/job, deterministic warnings/totals, regular-only bytes, and no goroutine/descriptor leak; rerun the focused command and `go test -race ./internal/scan -run '^(TestScannerFileCompletionPermutations|TestScannerHardLinkFinalization|TestScannerCancellationStopDrainJoin|TestScannerNoPipelineLeaks|TestScannerDeterministicWarnings)$' -count=1`, expecting PASS. <!-- sdd-owner: implementation -->
- [x] **WU5E2 REFACTOR:** Run `gofmt -w internal/scan/scanner.go internal/scan/scanner_test.go internal/scan/finalize.go internal/scan/finalize_test.go && go test ./internal/scan -run '^(TestScannerFileCompletionPermutations|TestScannerHardLinkFinalization|TestScannerCancellationStopDrainJoin|TestScannerNoPipelineLeaks|TestScannerDeterministicWarnings)$' -count=1 && go test -race ./internal/scan -count=1 && go test ./... -count=1 && go vet ./...`; expect PASS, record accepted-WU5E1B1/final hashes, <=400 accounting, runtime N/A, and rollback. WU6 remains blocked until this independent receipt passes. <!-- sdd-owner: implementation -->

### WU6A — Establish the Policy and Traversal-Limit Contract

**Forecast:** 210–285 additions, 5–20 deletions, 215–305 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU5E2 evidence `sha256:2d3dffdfaa2f726f05374aebc3ac43ffa3439fc49ab9745ad3fb47df104a4363`.  
**Allowed edit surfaces:** `internal/scan/descriptor.go`, `internal/scan/descriptor_test.go`, `internal/scan/scanner.go`, `internal/scan/scanner_test.go`; append-only OpenSpec evidence only otherwise. No Darwin adapter, finalizer, report, TUI, CLI, or lifecycle edit.  
**Start:** `WalkLimits` exposes only depth/descriptors and `NewScanner` defaults to one worker with worker-sized queues.  
**Finish:** one validated policy seam carries defaults of 8 workers, job/result queues of 256, one concurrently active root, 250,000 entries per root, and 64 MiB cumulative relative-path bytes per root into descriptor traversal; exact-boundary entry/path cases succeed, first-over-boundary cases stop new traversal and emit stable limit evidence without broad cancellation precedence work. Existing depth/descriptor semantics remain intact.  
**Focused command:** `go test ./internal/scan -run '^(TestDefaultScanLimits|TestScannerDefaultPolicy|TestScannerEntryLimit|TestScannerPathBudget)$' -count=1`.  
**Runtime harness evidence:** N/A — validated fake-walker counters and relative-path byte accounting are the bounded policy/limit harness; no CLI exists.  
**Rollback boundary:** restore accepted WU5E2 `scanner.go` hash `d2f93151ee8ead09e5c878359e2dad5b305c3494659652c5af6a88df10819822`, `scanner_test.go` hash `7c33daccb9b71322395b7e7d3d01d89ccff5829beb4bd2836764dc46deda02f3`, and accepted descriptor hashes `2586e570c30fce08471dc88ad89e51433864247734c50da06c77b990a8e5991c` / `f2ebb0847e76c497986efc234d642596b75f870875cfa1d5bc306a126cc15315`; this removes only WU6A policy/limit behavior and tests. The stale `aa191d41` descriptor marker is corrected because its missing `PathError.Path` cannot compile with the accepted descriptor test hash; `2586e570` is the parent-reconciled operational baseline.

- [x] **WU6A RED:** In `internal/scan/descriptor_test.go` and `internal/scan/scanner_test.go`, add `TestDefaultScanLimits`, `TestScannerDefaultPolicy`, `TestScannerEntryLimit`, and `TestScannerPathBudget` first; require exact defaults 8/256/256/1/250000/67108864, invalid zero/negative contract rejection, exact-boundary success, first-over-boundary stop, stable `entry_limit_reached`/`path_budget_reached` evidence, and no extra root/job after exhaustion, then run the focused command and capture failure caused only by the missing contract/default/enforcement. <!-- sdd-owner: implementation -->
- [x] **WU6A GREEN:** Extend `WalkLimits` and scanner construction with the minimum validated entry/path and worker/queue/root-policy seam, inject the exact defaults, pass limits unchanged to each descriptor root traversal, and enforce entry/path exhaustion without adding cancellation precedence, report fields, CLI behavior, or Darwin-specific policy. <!-- sdd-owner: implementation -->
- [x] **WU6A TRIANGULATE:** Add table cases for empty roots, one entry/one byte below and at each boundary, the first entry/path byte over it, multi-component UTF-8 byte accounting, reordered fact delivery, and combined entry-plus-path exhaustion; prove deterministic warning selection, no off-by-one acceptance, one serial root, and no facts/jobs after the first exhausted boundary. <!-- sdd-owner: implementation -->
- [x] **WU6A REFACTOR:** Run `gofmt -w internal/scan/descriptor.go internal/scan/descriptor_test.go internal/scan/scanner.go internal/scan/scanner_test.go && go test ./internal/scan -run '^(TestDefaultScanLimits|TestScannerDefaultPolicy|TestScannerEntryLimit|TestScannerPathBudget)$' -count=1 && go test -race ./internal/scan -count=1`; expect PASS, record exact hashes and additions+deletions ≤400, runtime N/A, rollback, and independent WU6A acceptance before WU6B. <!-- sdd-owner: implementation -->

### WU6B — Finalize Cancellation, Status Precedence, and Shutdown Order

**Forecast:** 245–300 additions, 0–5 deletions, 245–305 authored changed lines; independently capped at 400.  
**Depends on:** independently accepted WU6A.  
**Allowed edit surfaces:** `internal/scan/scanner.go`, `internal/scan/scanner_test.go`, `internal/scan/finalize.go`, `internal/scan/finalize_test.go`; append-only OpenSpec evidence only otherwise. No descriptor/Darwin contract, report, TUI, CLI, or lifecycle edit.  
**Start:** accepted WU6A supplies the bounded policy and entry/path-limit facts; broad cancellation/root-status precedence remains intentionally deferred.  
**Finish:** synchronized cancellation before the first root, during an active root, and after completed roots stops new opens/jobs/roots; accepted work drains, fixed workers join, active/later roots finalize correctly, and exact traversed-root precedence is `cancelled > partial > boundary_limited > scanned` while every lower-precedence warning remains. Limit/partial/boundary combinations, shutdown order, no leak, and completion-order determinism are proven without report or CLI work.  
**Focused command:** `go test ./internal/scan -run '^(TestScannerStatusPrecedence|TestScannerCancellationBeforeRoots|TestScannerCancellationDuringRoot|TestScannerCancellationAfterRoots|TestScannerNoLeakedWork|TestScannerCompletionOrderStable)$' -count=1`.  
**Runtime harness evidence:** N/A — channel-synchronized fake walker/file-port calls are the designed cancellation and shutdown harness before a CLI exists.  
**Rollback boundary:** restore the independently accepted WU6A snapshots of the four allowed scanner/finalizer files; this removes only WU6B cancellation/status/shutdown behavior and tests while retaining WU6A policy and descriptor-limit contract.

- [x] **WU6B RED:** In `internal/scan/scanner_test.go` and `internal/scan/finalize_test.go`, add deterministic channel-synchronized tests first for cancellation before roots, during an active metadata/file call, and after earlier roots; require completed roots preserved, active root `cancelled/cancelled_during_scan`, later roots `skipped/cancelled_before_start`, accepted facts drained, and no sleep-based timing, then run the focused command and capture only missing cancellation/status failures. <!-- sdd-owner: implementation -->
- [x] **WU6B GREEN:** Add context checks before each new root/open/job, immediate enqueue/root-start stop, bounded active-call completion, one close/drain/join sequence, cancellation warnings, later-root skipping, incomplete cancelled estimates, and exact `cancelled > partial > boundary_limited > scanned` derivation while retaining all observed lower-precedence warnings. <!-- sdd-owner: implementation -->
- [x] **WU6B TRIANGULATE:** Add repeated cancellation, blocked result channels, cancellation before/during/after roots, worker completion permutations, and partial/boundary/WU6A entry/path-limit combinations; prove no newly started root or post-cancel inspection, no discarded accepted fact, exact precedence, deterministic roots/warnings/totals, child-before-parent shutdown evidence where exposed, and no goroutine or descriptor leak. <!-- sdd-owner: implementation -->
- [x] **WU6B REFACTOR:** Run `gofmt -w internal/scan/scanner.go internal/scan/scanner_test.go internal/scan/finalize.go internal/scan/finalize_test.go && go test ./internal/scan -run '^(TestScannerStatusPrecedence|TestScannerCancellationBeforeRoots|TestScannerCancellationDuringRoot|TestScannerCancellationAfterRoots|TestScannerNoLeakedWork|TestScannerCompletionOrderStable)$' -count=1 && go test -race ./internal/scan -count=1`; expect PASS, record exact hashes and additions+deletions ≤400, runtime N/A, rollback, and independent WU6B acceptance before WU7. <!-- sdd-owner: implementation -->

### WU7 — Render Deterministic JSON and Text

**Depends on:** independently accepted WU6B.  
**Allowed edit surfaces:** `internal/report/json.go`, `internal/report/text.go`, `internal/report/report_test.go`, `internal/report/testdata/complete_v1.json.golden`, `internal/report/testdata/complete_v1.txt.golden`, `internal/report/testdata/partial_v1.json.golden`, `internal/report/testdata/partial_v1.txt.golden`, `internal/report/testdata/cancelled_v1.json.golden`, `internal/report/testdata/cancelled_v1.txt.golden`.  
**Start:** a finalized snapshot is available but has no presentation.  
**Finish:** ordered schema-v1 DTOs and deterministic human output represent every shared snapshot fact without recomputation or actionable language.  
**Focused command:** `go test ./internal/report -run '^(TestJSONReport|TestTextReport|TestReportDeterminism|TestReportEquivalence)$' -count=1`.  
**Runtime harness evidence:** N/A — renderers are pure byte-producing package boundaries and are not wired to process streams yet.  
**Rollback boundary:** remove the three report source/test files and six named goldens; scanner/core behavior remains unchanged and unpublished.

- [x] **WU7 RED:** Add fixed-snapshot report tests for complete, partial, and cancelled outcomes, exact top-level/nested field order, typed bytes/booleans, explicit unknowns, `related_path: null`, `[]` collections, one trailing newline, all five roots, and text/JSON fact equivalence; run the focused command and record missing-renderer/golden failures. <!-- sdd-owner: implementation -->
- [x] **WU7 GREEN:** Implement ordered DTO projection in `internal/report/json.go` and canonical text rendering in `internal/report/text.go`, then run `go test ./internal/report -run '^(TestJSONReport|TestTextReport|TestReportDeterminism|TestReportEquivalence)$' -update -count=1`; inspect all six allowed goldens and rerun the focused command without `-update`, expecting PASS. <!-- sdd-owner: implementation -->
- [x] **WU7 TRIANGULATE:** Add discovery/collection permutations and assertions for byte-identical JSON, JSON parseability, schema/outcome/completeness, risks, bases, warnings, prominent partial/cancelled text, APFS clone/snapshot/compression/hard-link caveats, CoreSimulator manual-review wording, and no deletion-safety/reclaim promise; rerun without `-update` and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU7 REFACTOR:** Run `gofmt -w internal/report/json.go internal/report/text.go internal/report/report_test.go && go test ./internal/report -count=1`; expect PASS without golden updates, map-backed serialization, absolute home paths, wall-clock/host/user/random fields, or presentation-derived scan facts. <!-- sdd-owner: implementation -->

### WU8 — Present the Shared Snapshot in a Read-Only TUI

**Depends on:** WU7.  
**Allowed edit surfaces:** `go.mod`, generated `go.sum`, `internal/tui/model.go`, `internal/tui/view.go`, `internal/tui/run.go`, `internal/tui/model_test.go`.  
**Start:** deterministic text/JSON reports pass.  
**Finish:** pinned Bubble Tea v2, Bubbles v2 viewport, and Lip Gloss v2 dependencies support a finalized-snapshot-only viewer with navigation/details/quit and safe terminal lifecycle.  
**Focused command:** `go test ./internal/tui -run '^(TestModelInit|TestModelUpdateNavigation|TestModelUpdateQuit|TestModelViewLanguage|TestModelViewOutcomes|TestRunRejectsNonInteractive)$' -count=1`.  
**Runtime harness evidence:** N/A — the viewer is not wired to the CLI yet; direct `Model.Update`/`View` evidence is the designed boundary and no full interactive harness is required unless a lifecycle regression cannot be proved directly.  
**Rollback boundary:** remove `internal/tui/model.go`, `view.go`, `run.go`, and `model_test.go`, then remove only the WU8 Charm direct/indirect dependency entries from `go.mod` and regenerate `go.sum`; text/JSON remain complete.

- [x] **WU8 RED:** Add direct model tests for pure `Init`, canonical category movement, h/j/k/l and arrows, page scrolling, Tab details, resize, q/Escape/Ctrl-C quit, complete/partial/cancelled views, and terminal rejection; run the focused command and record missing-model/dependency failures. <!-- sdd-owner: implementation -->
- [x] **WU8 GREEN:** Pin approved compatible v2 dependency versions in `go.mod`, implement snapshot-only model/update/view/runner files, use only the viewport component, and rerun the focused command; expect PASS with no scan command from `Init` and no command after quit. <!-- sdd-owner: implementation -->
- [x] **WU8 TRIANGULATE:** Assert prominent “Read-only scan,” “Manual review required,” and “Estimate—not guaranteed reclaimable space” wording, cancellation/incompleteness before facts, canonical area/warning agreement, and absence of cleanup/select/action/confirm/delete/remove/Trash affordances while allowing the required estimate disclaimer; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU8 REFACTOR:** Run `gofmt -w internal/tui/model.go internal/tui/view.go internal/tui/run.go internal/tui/model_test.go && go mod tidy && go test ./internal/tui -count=1`; expect PASS, inspect generated `go.sum` in the complete receipt, and verify the model stores no operation intent or alternate totals. <!-- sdd-owner: implementation -->

### WU9 — Orchestrate One Scan, Stable Streams, and Exit Classes

**Depends on:** WU8.  
**Allowed edit surfaces:** `go.mod`, generated `go.sum`, `internal/cli/command.go`, `internal/cli/run.go`, `internal/cli/command_test.go`.  
**Start:** scanner and all three presentations compile independently.  
**Finish:** Cobra accepts only `osdy scan [--format text|json|tui]`, scans once after validation, renders before writing, separates streams, and maps every stable exit class.  
**Focused command:** `go test ./internal/cli -run '^(TestCommandRejectsPathBeforeScan|TestCommandRejectsFormat|TestRunScansOnce|TestRunStreamsAndExitCodes|TestRunFailures|TestRunTUIValidation|TestRunDisposableFixtureJSON)$' -count=1`.  
**Runtime harness evidence:** `go test ./internal/cli -run '^TestRunDisposableFixtureJSON$' -count=1 -v` must PASS and report one injected scan under `t.TempDir()`, exactly one parseable JSON stdout document, empty unrelated diagnostics, and no real-home resolver call.  
**Rollback boundary:** remove the three CLI files, remove only the WU9 Cobra dependency entries from `go.mod`, and regenerate `go.sum`; scanner/report/TUI packages remain independently testable.

- [x] **WU9 RED:** Add injected scanner/renderer/viewer/terminal/stream tests for default text, formats, positional or path-like input, unsupported format, noninteractive TUI, one scan call, complete/partial/cancelled outcomes, unsupported environment, global init/finalize/render/write/viewer failures, and fixture JSON; run the focused command and record missing-command/orchestration failures. <!-- sdd-owner: implementation -->
- [x] **WU9 GREEN:** Pin Cobra in `go.mod`, implement silenced thin command validation and one-scan orchestration in `internal/cli`, render text/JSON fully before stdout, keep snapshot warnings in-band and diagnostics on stderr, and centralize codes 0/1/2/3/4/130; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU9 TRIANGULATE:** Add poison-scanner cases proving invalid path/format/noninteractive TUI fail before construction or traversal, root warnings never become code 1, cancellation outranks code 3, global failure emits no purported snapshot, JSON has one document/newline and no decoration, write/viewer failure never rescans, and signal cancellation starts no follow-up operation; rerun and expect PASS. <!-- sdd-owner: implementation -->
- [x] **WU9 REFACTOR:** Run `gofmt -w internal/cli/command.go internal/cli/run.go internal/cli/command_test.go && go mod tidy && go test ./internal/cli -count=1`; expect PASS, execute the stated runtime harness evidence command, and account for generated `go.sum` without counting its lines as authored. <!-- sdd-owner: implementation -->

### WU10 — Wire the Process and Run Final Acceptance

**Depends on:** WU9.  
**Allowed edit surfaces:** `cmd/osdy/main.go` only; verification may read all WU1–WU9 files and create disposable build/fixture artifacts outside the repository.  
**Start:** CLI orchestration passes but no production executable exists.  
**Finish:** `cmd/osdy/main.go` owns signal context/process exit only, and all formatting, focused tests, full tests, vet, JSON parsing, and disposable-home runtime evidence pass.  
**Focused command:** `go test ./cmd/osdy ./internal/cli -count=1`.  
**Runtime harness evidence:** run the exact disposable fixture script below; it must build before changing `HOME`, scan only the temporary home, emit canonical schema-v1 JSON with the five IDs, leave stderr empty, and preserve fixture content/mode/mtime.  
**Rollback boundary:** remove `cmd/osdy/main.go`; no schema, daemon, state, migration, or scanned filesystem recovery is required, and the WU9 package-level CLI remains testable.

- [x] **WU10 RED:** Before creating `cmd/osdy/main.go`, run `go test ./cmd/osdy -count=1`; record the expected non-zero “directory/package not found” result as the missing production entry-point boundary. Historical RED was observed during ODD-5A when the process/production APIs were absent. <!-- sdd-owner: implementation -->
- [x] **WU10 GREEN:** Implement `cmd/osdy/main.go` with `signal.NotifyContext`, stop signal delivery, production dependency construction, delegation to `internal/cli`, and `os.Exit` only; run `gofmt -w cmd/osdy/main.go && go test ./cmd/osdy ./internal/cli -count=1` and expect PASS with no scan/report logic in `main`. ODD-5A recorded the genuine GREEN and subsequent focused/race/full checks. <!-- sdd-owner: implementation -->
- [x] **WU10 TRIANGULATE:** Run the disposable runtime script below and expect exit 0, empty stderr, exactly one parseable JSON document, schema 1, complete outcome, canonical five area IDs, no absolute temporary-home path in JSON, and identical before/after fixture metadata. <!-- sdd-owner: implementation -->
- [x] **WU10 REFACTOR:** Run the final acceptance sequence below in order: explicit-file `gofmt`, all focused package tests, report JSON parseability/equivalence tests without `-update`, `go test ./...`, `go vet ./...`, then the disposable runtime script again; expect every command to pass/no-diagnostic and stop rather than weakening checks if any result fails. <!-- sdd-owner: implementation -->

#### WU10 disposable runtime script

```sh
set -euo pipefail
repo_root="$(pwd -P)"
case "$repo_root" in
  /Users/*) ;;
  *) printf '%s\n' "WU10 runtime script must run from a canonical /Users repository worktree" >&2; exit 1 ;;
esac
tmp="$(mktemp -d "$repo_root/.osdy-runtime.XXXXXXXX")"
trap 'rm -rf "$tmp"' EXIT

go build -o "$tmp/osdy" ./cmd/osdy
home="$tmp/home"
mkdir -p \
  "$home/.npm/pkg" \
  "$home/Library/Caches/Homebrew/formula" \
  "$home/.gradle/caches/module" \
  "$home/Library/Developer/Xcode/DerivedData/App" \
  "$home/Library/Developer/CoreSimulator/Devices/device"
printf 'npm fixture' >"$home/.npm/pkg/data"
printf 'brew fixture' >"$home/Library/Caches/Homebrew/formula/data"
printf 'gradle fixture' >"$home/.gradle/caches/module/data"
printf 'xcode fixture' >"$home/Library/Developer/Xcode/DerivedData/App/data"
printf 'sim fixture' >"$home/Library/Developer/CoreSimulator/Devices/device/data"

/usr/bin/find "$home" -exec /usr/bin/stat -f '%N|%HT|%z|%m|%Sp' {} \; | LC_ALL=C sort >"$tmp/before.meta"
HOME="$home" "$tmp/osdy" scan --format json >"$tmp/scan.json" 2>"$tmp/scan.stderr"
test ! -s "$tmp/scan.stderr"
python3 - "$tmp/scan.json" "$home" "$tmp" <<'PY'
import json
import pathlib
import sys

path = pathlib.Path(sys.argv[1])
home = sys.argv[2]
temporary_root = sys.argv[3]
raw = path.read_text(encoding="utf-8")
doc = json.loads(raw)
assert raw.endswith("\n")
assert home not in raw
assert temporary_root not in raw
assert doc["schema_version"] == 1
assert doc["outcome"] == "complete"
assert doc["complete"] is True
assert [root["area_id"] for root in doc["roots"]] == [
    "npm-cache",
    "homebrew-cache",
    "gradle-caches",
    "xcode-derived-data",
    "core-simulator",
]
PY
/usr/bin/find "$home" -exec /usr/bin/stat -f '%N|%HT|%z|%m|%Sp' {} \; | LC_ALL=C sort >"$tmp/after.meta"
cmp "$tmp/before.meta" "$tmp/after.meta"
```

#### WU10 final acceptance sequence

```sh
gofmt -w \
  cmd/osdy/main.go \
  internal/core/types.go internal/core/types_test.go \
  internal/core/snapshot.go internal/core/snapshot_test.go \
  internal/scan/builtins.go internal/scan/builtins_test.go \
  internal/scan/scanner.go internal/scan/scanner_test.go \
  internal/scan/finalize.go internal/scan/finalize_test.go \
  internal/platform/macos/metadata_darwin.go \
  internal/platform/macos/metadata_other.go \
  internal/platform/macos/metadata_darwin_test.go \
  internal/platform/macos/metadata_other_test.go \
  internal/report/json.go internal/report/text.go internal/report/report_test.go \
  internal/tui/model.go internal/tui/view.go internal/tui/run.go internal/tui/model_test.go \
  internal/cli/command.go internal/cli/run.go internal/cli/command_test.go

go test ./internal/core -count=1
go test ./internal/scan -count=1
go test ./internal/platform/macos -count=1
go test ./internal/report -count=1
go test ./internal/tui -count=1
go test ./internal/cli -count=1
go test ./cmd/osdy -count=1
go test ./internal/report -run '^(TestJSONReport|TestReportDeterminism|TestReportEquivalence)$' -count=1
go test ./... -count=1
go vet ./...
```

After this sequence, rerun the WU10 disposable runtime script. The report-focused command and the script's `json.loads` call are the explicit JSON parseability checks; neither may use or scan the real home.

## Parent SDD Orchestration Decisions and Evidence

These actions use SDD apply/verify authority only. They do not launch ordinary review or lifecycle gates.

- [x] Before resumed apply, record the user-selected `feature-branch-chain` strategy with unavoidable cohesive-slice `size:exception`, retain the WU2A → WU2B split and module import identity, and preserve one-writer execution. <!-- sdd-owner: parent -->
- [x] After each applied work unit, inspect its apply-progress receipt, confirm authored additions plus deletions are at most 400 with generated `go.sum` excluded, confirm the focused command/result, runtime evidence or explicit N/A, complete changed-file identity, and rollback boundary, then authorize the next dependent unit through SDD apply/verify authority; oversized combined units were retained only as nonexecuting provenance and split before implementation. <!-- sdd-owner: parent -->
- [x] After WU10, record final SDD completion evidence against the normative coverage matrix, required commands, corrected disposable runtime script, authored-line accounting, changed-file identity, and rollback boundaries; implementation completion is supported by ODD and OpenSpec reconciliation evidence. <!-- sdd-owner: parent -->

## Current Ledger Superseding Addendum

The controlling recount is **158 total = 119 checked + 39 unchecked; 155 implementation-owned = 119 checked + 36 unchecked; 3 parent-owned = 0 checked + 3 unchecked**. Every checkbox has exactly one terminal ownership marker, and this acceptance-only update changed no checkbox state or ownership marker.

WU5E1A is accepted at exact **+400/-0=400** by independent verifier `subtask_gentle-ai-verify_1788146646060_00f9245a` PASS, parent LSP 0 errors, clean parent lens diagnostics, and settlement state `complete`. Its final hashes are `descriptor.go` `aa191d41ad70864d3bc7ca954fbe8f6c31f7970693dfe7573460d17a5616d620`, `descriptor_test.go` `f2ebb0847e76c497986efc234d642596b75f870875cfa1d5bc306a126cc15315`, `scanner.go` `672f14c0e410c9aef07b5eacadec53f6b8524e484a8da266c43c87f40342deb6`, and `scanner_test.go` `6abfa3912d62446a74f207f227832379c79c6df9c92134cdf152bab55acd6417`.

The original WU5E1 remains untouched historical oversized/nonexecuting context. WU5E1B is the next implementation unit and depends on accepted E1A; WU5E2 remains blocked until E1B is independently accepted. The seven D2A1 files remain staged while E1A working changes are unstaged. No commit or push exists.

## WU6 Split Ledger Superseding Addendum

The controlling recount after the explicitly authorized planning-only split is **170 total = 135 checked + 35 unchecked; 167 implementation-owned = 135 checked + 32 unchecked; 3 parent-owned = 0 checked + 3 unchecked**. The four original unchecked WU6 implementation rows are superseded in place by eight unchecked WU6A/WU6B implementation rows; no implementation checkbox was falsely checked, all WU5E2 rows remain checked, and every other checkbox retains its prior state, order, and terminal ownership marker.

WU6A and WU6B are separate `stacked-to-main` work units, each forecast at no more than 305 authored changed lines and hard-capped at 400. WU6B cannot start before independent WU6A acceptance, and WU7 cannot start before independent WU6B acceptance. This addendum supersedes only older ledger/dependency prose; it records no Go/test implementation, runtime execution, lifecycle operation, or acceptance.
